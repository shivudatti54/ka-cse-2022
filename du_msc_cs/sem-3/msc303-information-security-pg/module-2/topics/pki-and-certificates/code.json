{
  "topicId": "pki-and-certificates",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Certificate Validation",
      "description": "Basic certificate chain verification logic",
      "code": "FUNCTION validate_cert(cert, trust_store):\n  WHILE cert.issuer \u2260 trust_store:\n    cert = get_parent_cert(cert)\n    IF not verify_signature(cert): RETURN False\n  RETURN True",
      "explanation": "Traverses certificate chain until trusted root is found, verifying signatures at each level",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}