{
  "topicId": "blockchain-security",
  "examples": [
    {
      "id": "code-1",
      "language": "Python",
      "title": "SHA-256 Hash Example",
      "description": "Demonstrates cryptographic hashing for block headers",
      "code": "import hashlib\ndef hash_block(header):\n    return hashlib.sha256(header.encode()).hexdigest()",
      "explanation": "Converts block header to fixed-size unique hash",
      "timeComplexity": "O(1)",
      "spaceComplexity": "O(1)"
    }
  ]
}