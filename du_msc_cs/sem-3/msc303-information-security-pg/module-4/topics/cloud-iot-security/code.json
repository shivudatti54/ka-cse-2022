{
  "topicId": "cloud-iot-security",
  "examples": [
    {
      "id": "code-1",
      "language": "python",
      "title": "Device Attestation",
      "description": "Secure boot verification using cryptographic hashing",
      "code": "def verify_firmware(fw, signature, public_key):\n    hash = SHA256(fw).digest()\n    return rsa.verify(hash, signature, public_key)",
      "explanation": "Validates firmware integrity before installation",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}