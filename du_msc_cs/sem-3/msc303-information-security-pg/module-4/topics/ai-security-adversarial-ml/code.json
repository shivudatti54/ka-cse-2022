{
  "topicId": "ai-security-adversarial-ml",
  "examples": [
    {
      "id": "code-1",
      "language": "python",
      "title": "FGSM Implementation",
      "description": "Generates adversarial examples using Fast Gradient Sign Method",
      "code": "import torch\ndef fgsm_attack(image, epsilon, data_grad):\n    sign_grad = data_grad.sign()\n    perturbed_image = image + epsilon * sign_grad\n    return torch.clamp(perturbed_image, 0, 1)",
      "explanation": "Computes gradient sign and adds scaled perturbation to input",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}