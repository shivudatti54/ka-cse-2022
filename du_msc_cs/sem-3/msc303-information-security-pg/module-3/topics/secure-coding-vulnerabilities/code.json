{
  "topicId": "secure-coding-vulnerabilities",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Secure Input Validation",
      "description": "Demonstrates parameterized query usage",
      "code": "FUNCTION login(username, password)\n    query = \"SELECT * FROM users WHERE username = ? AND password = ?\"\n    EXECUTE query WITH (username, password)",
      "explanation": "Prevents SQL injection through query parameterization",
      "timeComplexity": "O(1)",
      "spaceComplexity": "O(n)"
    }
  ]
}