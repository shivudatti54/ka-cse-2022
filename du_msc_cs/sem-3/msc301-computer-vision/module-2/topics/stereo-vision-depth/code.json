{
  "topicId": "stereo-vision-depth",
  "examples": [
    {
      "id": "code-1",
      "language": "Python",
      "title": "Disparity Calculation",
      "description": "Block matching using SAD",
      "code": "def compute_disparity(left, right, block_size=5, max_d=64):\n    h, w = left.shape\n    disparity = np.zeros_like(left)\n    for y in range(block_size, h-block_size):\n        for x in range(block_size, w-block_size):\n            # SAD implementation\n    return disparity",
      "explanation": "Sliding window approach comparing pixel intensity blocks",
      "timeComplexity": "O(n\u00b2m\u00b2)",
      "spaceComplexity": "O(n\u00b2)"
    }
  ]
}