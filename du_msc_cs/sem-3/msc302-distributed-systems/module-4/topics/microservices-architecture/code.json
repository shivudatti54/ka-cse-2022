{
  "topicId": "microservices-architecture",
  "examples": [
    {
      "id": "code-1",
      "language": "python",
      "title": "Service Discovery Client",
      "description": "Client-side service discovery implementation",
      "code": "import requests\n\ndef get_service(service_name):\n    registry = requests.get('http://registry:8500/services')\n    instances = registry.json()[service_name]\n    return load_balancer.select(instances)",
      "explanation": "Fetches available instances from registry and applies load balancing",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}