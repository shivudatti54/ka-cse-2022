{
  "topicId": "consensus-paxos-raft",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Basic Paxos",
      "description": "Core logic for proposal acceptance",
      "code": "PROPOSER:\n  send PREPARE(n)\n  if majority PROMISE(n, v):\n    send ACCEPT(n, v)\nACCEPTOR:\n  if n > highest_seen:\n    promise(n)\n  if accept(n,v):\n    send ACCEPTED(n,v)",
      "explanation": "Proposer gets promises from majority, then sends accept request.",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}