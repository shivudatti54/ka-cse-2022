{
  "topicId": "spark-streaming-flink",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Word Count in Spark Streaming",
      "description": "Micro-batch word frequency calculation",
      "code": "stream = ssc.socketTextStream(...)\ncounts = stream.flatMap(...).map(...).reduceByKey(...)\ncounts.print()",
      "explanation": "Processes text in fixed time windows",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(k) where k=unique words"
    },
    {
      "id": "code-2",
      "language": "Java",
      "title": "Flink Windowed Aggregation",
      "description": "Event-time window processing",
      "code": "DataStream<Event> stream = ...;\nstream.keyBy(...)\n     .window(TumblingEventTimeWindows.of(...))\n     .sum(...);",
      "explanation": "Handles out-of-order events using watermarks",
      "timeComplexity": "O(1) per event",
      "spaceComplexity": "O(m) for window state"
    }
  ]
}