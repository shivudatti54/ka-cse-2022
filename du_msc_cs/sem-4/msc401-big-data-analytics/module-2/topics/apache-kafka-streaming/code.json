{
  "topicId": "apache-kafka-streaming",
  "examples": [
    {
      "id": "code-1",
      "language": "Java",
      "title": "Basic Kafka Producer",
      "description": "Demonstrates message publishing",
      "code": "Properties props = new Properties();\nprops.put(\"bootstrap.servers\", \"localhost:9092\");\nprops.put(\"key.serializer\", \"StringSerializer\");\nProducer<String, String> producer = new KafkaProducer<>(props);\nproducer.send(new ProducerRecord<>(\"topic\", \"key\", \"value\"));",
      "explanation": "Configures producer properties and sends message to topic",
      "timeComplexity": "O(1) per message",
      "spaceComplexity": "O(n) for message buffer"
    }
  ]
}