{
  "topicId": "multipath-tcp",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "MPTCP Packet Scheduling",
      "description": "Basic round-robin scheduler for subflows",
      "code": "FUNCTION schedule_packet(packets):\n    FOR each subflow IN active_subflows:\n        IF subflow.has_capacity():\n            RETURN subflow\n    RETURN first_available()",
      "explanation": "Iterates through subflows checking available capacity",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}