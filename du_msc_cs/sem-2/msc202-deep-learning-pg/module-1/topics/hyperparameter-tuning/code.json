{
  "topicId": "hyperparameter-tuning",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Random Search",
      "description": "Basic random search implementation",
      "code": "ALGORITHM RandomSearch\nBEGIN\n  FOR i IN 1..num_iterations:\n    params = sample_hyperparameters()\n    score = cross_validate(params)\n    keep_best(score, params)\nEND",
      "explanation": "Randomly samples hyperparameters and keeps best performing",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}