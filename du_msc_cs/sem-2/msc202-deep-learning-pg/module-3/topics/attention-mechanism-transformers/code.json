{
  "topicId": "attention-mechanism-transformers",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Scaled Dot-Product Attention",
      "description": "Core attention mechanism implementation",
      "code": "function attention(Q, K, V):\n    scores = matmul(Q, K.T) / sqrt(d_k)\n    weights = softmax(scores)\n    return matmul(weights, V)",
      "explanation": "Computes attention weights and applies them to values",
      "timeComplexity": "O(n\u00b2*d)",
      "spaceComplexity": "O(n\u00b2)"
    }
  ]
}