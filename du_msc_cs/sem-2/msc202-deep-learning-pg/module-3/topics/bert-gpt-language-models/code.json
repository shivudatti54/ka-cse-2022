{
  "topicId": "bert-gpt-language-models",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "BERT Masked LM",
      "description": "Masked language model training step",
      "code": "FOR each sentence in batch:\n  MASK 15% tokens\n  ENCODE with Transformer\n  CALCULATE loss on masked positions",
      "explanation": "Demonstrates BERT's core pre-training objective",
      "timeComplexity": "O(n^2)",
      "spaceComplexity": "O(n)"
    },
    {
      "id": "code-2",
      "language": "Python",
      "title": "GPT Text Generation",
      "description": "Autoregressive text generation with temperature",
      "code": "def generate(prompt, max_length=50, temperature=1.0):\n  tokens = tokenize(prompt)\n  for _ in range(max_length):\n    logits = model(tokens)\n    next_token = sample(logits[-1], temperature)\n    tokens.append(next_token)\n  return detokenize(tokens)",
      "explanation": "Shows iterative token prediction with temperature control",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}