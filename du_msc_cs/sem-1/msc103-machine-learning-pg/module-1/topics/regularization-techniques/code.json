{
  "topicId": "regularization-techniques",
  "examples": [
    {
      "id": "code-1",
      "language": "python",
      "title": "Ridge Regression",
      "description": "Implementation using scikit-learn",
      "code": "from sklearn.linear_model import Ridge\nmodel = Ridge(alpha=0.5)\nmodel.fit(X_train, y_train)",
      "explanation": "Alpha parameter controls regularization strength",
      "timeComplexity": "O(n_samples * n_features)",
      "spaceComplexity": "O(n_features)"
    }
  ]
}