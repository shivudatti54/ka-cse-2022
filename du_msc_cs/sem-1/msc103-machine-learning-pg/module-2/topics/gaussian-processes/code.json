{
  "topicId": "gaussian-processes",
  "examples": [
    {
      "id": "code-1",
      "language": "Python",
      "title": "GP Regression with scikit-learn",
      "description": "Basic implementation of GP regression",
      "code": "from sklearn.gaussian_process import GaussianProcessRegressor\nfrom sklearn.gaussian_process.kernels import RBF\n\nkernel = RBF(length_scale=1.0)\ngpr = GaussianProcessRegressor(kernel=kernel)\ngpr.fit(X_train, y_train)\ny_pred, y_std = gpr.predict(X_test, return_std=True)",
      "explanation": "Implements GP regression with RBF kernel and returns predictions with uncertainty",
      "timeComplexity": "O(n\u00b3)",
      "spaceComplexity": "O(n\u00b2)"
    }
  ]
}