{
  "topicId": "index-structures-b-trees-hash",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "B-Tree Search",
      "description": "Demonstrates key lookup in B-Tree",
      "code": "FUNCTION search(node, key):\n    WHILE node not leaf:\n        FIND first i where key < node.keys[i]\n        node = node.children[i]\n    RETURN key in node.keys",
      "explanation": "Traverses from root to leaf following key comparisons",
      "timeComplexity": "O(log n)",
      "spaceComplexity": "O(1)"
    }
  ]
}