{
  "topicId": "query-processing-optimization",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Merge Join Algorithm",
      "description": "Joins two sorted relations",
      "code": "PROCEDURE MergeJoin(R, S)\n  WHILE !R.EOF AND !S.EOF\n    IF R.key == S.key: OUTPUT tuple\n    ELSE IF R.key < S.key: R.ADVANCE\n    ELSE: S.ADVANCE\nEND",
      "explanation": "Simultaneously scans both sorted inputs matching keys",
      "timeComplexity": "O(m+n)",
      "spaceComplexity": "O(1)"
    }
  ]
}