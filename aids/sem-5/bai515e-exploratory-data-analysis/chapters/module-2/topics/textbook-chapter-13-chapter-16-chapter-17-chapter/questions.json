{
  "questions": [
    {
      "question": "What is the purpose of using `dropna()` function in pandas to handle missing data?",
      "type": "short_answer",
      "marks": 5,
      "hint": "Optional hint"
    },
    {
      "question": "Write a Python code snippet to merge two datasets using the `merge()` function, one with 'left' and one with 'right' on the 'key' column.",
      "type": "long_answer",
      "marks": 15,
      "hint": "Optional hint"
    },
    {
      "question": "What is the difference between the `isnull()` and `isna()` functions in pandas when handling missing data?",
      "type": "short_answer",
      "marks": 5,
      "hint": "Optional hint"
    }
  ]
}
