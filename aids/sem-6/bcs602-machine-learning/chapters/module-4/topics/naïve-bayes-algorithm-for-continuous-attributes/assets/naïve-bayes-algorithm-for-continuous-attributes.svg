<svg width="420" height="400" data-topic-id="naive-bayes-algorithm-for-continuous-attributes">
    <title>Naïve Bayes Algorithm for Continuous Attributes</title>
    <defs>
        <style>
            .pastel-gradient {
                background: linear-gradient(to bottom, #C7B8EA, #C7B8EA, #C7B8EA);
                background-size: 100% 100%;
                background-origin: padding-box;
                -webkit-background-clip: padding-box;
                -moz-background-clip: padding-box;
                clip: padding-box;
            }
        </style>
    </defs>
    <background class="pastel-gradient" data-narration="background-narration"></background>
    <section data-narration="intro-narration" class="section">
        <div class="label">What is Naïve Bayes?</div>
        <div class="text">
            Naïve Bayes is a family of probabilistic classifiers based on Bayes' theorem, which is a mathematical formula for determining the probability of an event based on the probabilities of its constituent parts. The algorithm is called "naïve" because it assumes that the features are independent, meaning that the value of one feature does not affect the value of another feature.
        </div>
        <div class="arrow"></div>
        <div class="label">Key Concepts</div>
        <div class="text">
            <ul>
                <li>Conditional Probability: The probability of an event occurring given that another event has occurred.</li>
                <li>Bayes' Theorem: The probability of an event happening given some prior knowledge of conditions that might be related to the event.</li>
                <li>Naïve Assumption: The assumption that each feature is independent of every other feature.</li>
            </ul>
        </div>
        <div class="arrow"></div>
        <div class="label">Naïve Bayes Classifier</div>
        <div class="text">
            The Naïve Bayes classifier uses the following steps to classify new data points:
            <ul>
                <li>Calculate Prior Probabilities: Calculate the prior probability of each class.</li>
                <li>Calculate Likelihoods: Calculate the likelihood of each feature given each class.</li>
                <li>Calculate Posterior Probabilities: Calculate the posterior probability of each class given each feature.</li>
                <li>Classify: Choose the class with the highest posterior probability.</li>
            </ul>
        </div>
        <div class="arrow"></div>
        <div class="label">Naïve Bayes for Continuous Attributes</div>
        <div class="text">
            To implement Naïve Bayes for continuous attributes, we need to calculate the likelihood of each feature given each class. We can do this by using the following formula:
            <pre>P(X=x|y=c) = (1/σ(y))^n \* f(x) \* P(y=c)</pre>
            where:
            <ul>
                <li>P(X=x|y=c) is the probability of feature x given class c</li>
                <li>σ(y) is the standard deviation of class y</li>
                <li>n is the number of features</li>
                <li>f(x) is the probability density</li>
            </ul>
        </div>
    </section>
    <section data-narration="likelihoods-narration" class="section">
        <div class="label">Calculate Likelihoods</div>
        <div class="text">
            The likelihood of each feature given each class is calculated using the following formula:
            <pre>P(X=x|y=c) = (1/σ(y))^n \* f(x) \* P(y=c)</pre>
            where:
            <ul>
                <li>P(X=x|y=c) is the probability of feature x given class c</li>
                <li>σ(y) is the standard deviation of class y</li>
                <li>n is the number of features</li>
                <li>f(x) is the probability density</li>
                <li>P(y=c) is the prior probability of class c</li>
            </ul>
        </div>
        <div class="arrow"></div>
        <div class="label">Posterior Probabilities</div>
        <div class="text">
            The posterior probability of each class given each feature is calculated using Bayes' theorem:
            <pre>P(y=c|X=x) = P(X=x|y=c) \* P(y=c) / P(X=x)</pre>
            where:
            <ul>
                <li>P(y=c|X=x) is the posterior probability of class c given feature x</li>
                <li>P(X=x|y=c) is the likelihood of feature x given class c</li>
                <li>P(y=c) is the prior probability of class c</li>
                <li>P(X=x) is the marginal probability of feature x</li>
            </ul>
        </div>
        <div class="arrow"></div>
        <div class="label">Classify</div>
        <div class="text">
            The class with the highest posterior probability is chosen as the predicted class:
            <pre>P(y=c) = P(X=x|y=c) \* P(y=c) / P(X=x)</pre>
        </div>
    </section>
    <section data-narration="final-classification-narration" class="section">
        <div class="label">Final Classification</div>
        <div class="text">
            The Naïve Bayes classifier uses the following steps to classify new data points:
            <ul>
                <li>Calculate Prior Probabilities: Calculate the prior probability of each class.</li>
                <li>Calculate Likelihoods: Calculate the likelihood of each feature given each class.</li>
                <li>Calculate Posterior Probabilities: Calculate the posterior probability of each class given each feature.</li>
                <li>Classify: Choose the class with the highest posterior probability.</li>
            </ul>
        </div>
        <div class="arrow"></div>
        <footer data-narration="footer-narration">Naïve Bayes Algorithm for Continuous Attributes</footer>
    </section>
    <animate xmlns="http://www.w3.org/2000/svg" dur="1000ms" repeatCount="indefinite">
        <animate transform="rotate(360deg)" begin="0s" duration="500ms" fill="freeze"></animate>
    </animate>
</svg>