<svg width="420" height="400" data-topic-id="bayesian-learning-1011" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
  <!-- Gradient background -->
  <Defs>
    <LinearGradient id="pastelGradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#C7B8EA" stop-opacity="1"/>
      <stop offset="100%" stop-color="#87CEEB" stop-opacity="1"/>
    </LinearGradient>
  </Defs>
  <rect fill="url(#pastelGradient)" width="100%" height="100%"/>
  <!-- Title -->
  <text x="15" y="20" font-size="24" font-family="Arial" fill="#3498db" text-anchor="start">
    Bayesian Learning: Bayes' Theorem
  </text>
  <!-- Section 1: Bayes' Theorem -->
  <rect x="15" y="60" width="390" height="80" fill="#fff" rx="10" data-narration="Bayes' Theorem is a mathematical formula that describes the probability of a hypothesis given some new data. P(H|D) = P(D|H) \* P(H) / P(D)"/>
  <text x="15" y="80" font-size="18" font-family="Arial" fill="#2ecc71" text-anchor="start">
    P(H|D) = P(D|H) \* P(H) / P(D)
  </text>
  <animate transform="rotate(360 20 100)" begin="0s" dur="4s" repeatCount="indefinite"/>
  <!-- Section 2: Prior Probability -->
  <rect x="15" y="150" width="390" height="80" fill="#fff" rx="10" data-narration="The prior probability of a hypothesis is the probability of the hypothesis before any new data is observed. For example, P(Car) = 0.5, which means that we think it is equally likely that a person owns a car or not."]
  <text x="15" y="170" font-size="18" font-family="Arial" fill="#2ecc71" text-anchor="start">
    Prior Probability
  </text>
  <animate transform="rotate(360 20 150)" begin="0s" dur="4s" repeatCount="indefinite"/>
  <!-- Section 3: Likelihood Function -->
  <rect x="15" y="220" width="390" height="80" fill="#fff" rx="10" data-narration="The likelihood function is the probability of the data given the hypothesis. For example, P(D|Car) = 0.8, which means that if a person owns a car, then the probability of them having a certain type of insurance is 0.8."]
  <text x="15" y="240" font-size="18" font-family="Arial" fill="#2ecc71" text-anchor="start">
    Likelihood Function
  </text>
  <animate transform="rotate(360 20 220)" begin="0s" dur="4s" repeatCount="indefinite"/>
  <!-- Section 4: Posterior Probability -->
  <rect x="15" y="280" width="390" height="80" fill="#fff" rx="10" data-narration="The posterior probability of a hypothesis is the probability of the hypothesis given the data. P(H|D) = P(D|H) \* P(H) / P(D) can be used to update the prior probability based on the new data."]
  <text x="15" y="300" font-size="18" font-family="Arial" fill="#2ecc71" text-anchor="start">
    Posterior Probability
  </text>
  <animate transform="rotate(360 20 280)" begin="0s" dur="4s" repeatCount="indefinite"/>
  <!-- Arrow markers -->
  <path d="M 20 100 L 40 120" fill="none" stroke="#3498db" stroke-width="2"/>
  <path d="M 20 100 L 60 120" fill="none" stroke="#3498db" stroke-width="2"/>
  <!-- Footer -->
  <text x="15" y="380" font-size="18" font-family="Arial" fill="#2ecc71" text-anchor="start">
    Bayesian Learning: Bayes Theorem
  </text>
</svg>