{
  "mnemonics": ["MongoDB is Like a Tree", "Each node is a document"],
  "keyPoints": [
    "MongoDB is Like a Tree",
    "Each node is a document",
    "Document is like a box that can contain",
    "    - strings (like strings in RDBMS)",
    "    - numbers (like numbers in RDBMS)",
    "    - dates (like dates in RDBMS)",
    "    - booleans (like booleans in RDBMS)",
    "    - arrays (like arrays in RDBMS)",
    "    - objects (like objects in RDBMS)",
    "    - binary data (like binary data in RDBMS)",
    "    - null (like null in RDBMS)"
  ]
}
