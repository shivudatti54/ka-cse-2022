<svg width="420" height="400" data-topic-id="greedy-best-first-search">
  <!-- Gradient background with pastel colors -->
  <defs>
    <linearGradient id="pastel-gradient" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#C7B8EA" stop-opacity="1" />
      <stop offset="100%" stop-color="#ACC2E2" stop-opacity="1" />
    </linearGradient>
  </defs>
  <rect x="0" y="0" width="420" height="400" style="fill: url(#pastel-gradient)" />
  
  <!-- Title at top -->
  <g transform="translate(10, 10)">
    <text style="font-size: 24px; font-weight: bold;" text-anchor="start">Greedy Best First Search</text>
  </g>
  
  <!-- Section 1: Introduction -->
  <g transform="translate(50, 100)">
    <rect width="300" height="50" style="stroke: #000; stroke-width: 2;" fill="none" rx="5" />
    <text style="font-size: 18px;" text-anchor="start" data-narration="Introduction: GBFS is an algorithm that selects the next node to visit based on the estimated total cost of reaching the goal node, rather than the estimated cost of reaching the goal node from the current node." x="10" y="10">GBFS Algorithm</text>
  </g>
  
  <!-- Section 2: How GBFS Works -->
  <g transform="translate(50, 150)">
    <rect width="300" height="50" style="stroke: #000; stroke-width: 2;" fill="none" rx="5" />
    <text style="font-size: 18px;" text-anchor="start" data-narration="How GBFS Works: The algorithm starts with the initial node and estimates the cost of reaching each neighbor based on the edge weights, then selects the next node to visit based on the estimated total cost of reaching the goal node from each neighbor." x="10" y="10">How GBFS Works</text>
  </g>
  
  <!-- Section 3: Key Concepts -->
  <g transform="translate(50, 200)">
    <rect width="300" height="50" style="stroke: #000; stroke-width: 2;" fill="none" rx="5" />
    <text style="font-size: 18px;" text-anchor="start" data-narration="Key Concepts: Heuristic, cost, and estimated total cost are key concepts in GBFS. The heuristic is used to estimate the cost of reaching the goal node from a given node, the cost is the sum of the edge weights, and the estimated total cost is the sum of the cost and the estimated cost of reaching the goal node from that node." x="10" y="10">Key Concepts</text>
  </g>
  
  <!-- Example -->
  <g transform="translate(50, 250)">
    <rect width="300" height="50" style="stroke: #000; stroke-width: 2;" fill="none" rx="5" />
    <text style="font-size: 18px;" text-anchor="start" data-narration="Example: Suppose we have a weighted graph with the following nodes and edges: | Node | Edge Weight | | ---- | ----------- | | A    | 1            | | B    | 2            | | C    | 3            | | D    | 4            | | E    | 5            | The shortest path from A to E is A -> B -> C -> D -> E." x="10" y="10">Example</text>
  </g>
  
  <!-- Animation -->
  <animate attributeName="opacity" begin="1s" dur="3s" repeatCount="indefinite" />
  <animate attributeName="transform" begin="2s" dur="3s" transform="translate(50, 300)" />
  <animate attributeName="transform" begin="4s" dur="3s" transform="translate(50, 400)" />
</svg>