{
  "type": "flowchart",
  "title": "The GROUP BY Process Flow",
  "description": "A flowchart illustrating the logical steps and outcome of using the GROUP BY clause in SQL to aggregate data.",
  "elements": [
    "Raw Data Table (Rows)",
    "Grouping Key (e.g., 'department')",
    "Aggregate Function (e.g., SUM(salary), COUNT(*))",
    "Result Set (Grouped & Aggregated Rows)"
  ],
  "relationships": [
    "Raw data is partitioned into groups based on the grouping key",
    "An aggregate function is applied to each individual group",
    "A single result row is produced for each group"
  ]
}