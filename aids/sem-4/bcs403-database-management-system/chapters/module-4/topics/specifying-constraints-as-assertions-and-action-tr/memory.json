{
  "mnemonics": [" CHECKS LIKE CHECK", "ASSERT Actions ARE TRiggers"],
  "keyPoints": [
    "Constraints can be specified as assertions to ensure data integrity.",
    "Constraints can also be specified as triggers to take action after each row is inserted, updated, or deleted.",
    "Assertions are like checks that validate data upon insertion or update, while triggers are like actions that take place after data changes."
  ]
}
