{
  "topicId": "the-pigeon-hole-principle",
  "questions": [
    {
      "id": "q1",
      "type": "theory",
      "question": "Explain the Open/Closed Principle with an example",
      "answer": "Open/Closed Principle (OCP):\n\"Software entities should be open for extension but closed for modification.\"\n\nExample - Shape Area Calculation:\n\nBAD (violates OCP):\nclass AreaCalculator {\n  double calculate(Object shape) {\n    if (shape instanceof Circle) {...}\n    else if (shape instanceof Square) {...}\n    // Adding Triangle requires modifying this class!\n  }\n}\n\nGOOD (follows OCP):\ninterface Shape {\n  double area();\n}\nclass Circle implements Shape { ... }\nclass Square implements Shape { ... }\n// Adding Triangle: just create new class, no modification needed\n\nBenefit: New shapes can be added without changing existing code."
    }
  ]
}