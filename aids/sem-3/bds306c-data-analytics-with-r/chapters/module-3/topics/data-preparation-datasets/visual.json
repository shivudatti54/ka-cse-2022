{
  "type": "flowchart",
  "title": "Data Preparation Pipeline in R",
  "description": "A flowchart illustrating the sequential and iterative steps for preparing a raw dataset for analysis using R.",
  "elements": [
    "Import Data (read.csv, readxl)",
    "Inspect Data (str(), summary())",
    "Handle Missing Values (na.omit(), is.na())",
    "Correct Data Types (as.factor(), as.numeric())",
    "Create/Transform Variables (mutate())",
    "Filter & Subset Data (filter(), select())",
    "Aggregate Data (group_by(), summarize())",
    "Tidy/Reshape Data (pivot_longer(), pivot_wider())",
    "Export Clean Data (write.csv())"
  ],
  "relationships": [
    "A sequential process where the output of one step is the input for the next, with potential for iteration between steps like inspection and transformation."
  ]
}