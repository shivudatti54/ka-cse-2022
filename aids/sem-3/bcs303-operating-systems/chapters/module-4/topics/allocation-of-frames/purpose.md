# Why Study Allocation Methods?

Understanding how files are stored on disk (contiguous, linked, indexed) is fundamental to file system design. The Unix inode structure with multi-level indexing is a classic exam topic.
