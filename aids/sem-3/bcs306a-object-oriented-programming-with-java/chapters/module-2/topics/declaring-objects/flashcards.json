{
  "content": "```json\n[\n  {\n    \"id\": \"f1\",\n    \"front\": \"What is the difference between declaring a variable and creating an object in Java?\",\n    \"back\": \"Declaring a variable (e.g., `MyClass obj;`) simply creates a reference that can point to an object of type `MyClass`. Creating an object (e.g., `obj = new MyClass();`) uses the `new` keyword to instantiate the object in memory, and the assignment operator links the reference to that new object.\"\n  },\n  {\n    \"id\": \"f2\",\n    \"front\": \"What is the purpose of the `new` keyword in Java?\",\n    \"back\": \"The `new` keyword is an operator that dynamically allocates memory (on the heap) for a new object during runtime. It also calls the class's constructor to initialize the newly created object.\"\n  },\n  {\n    \"id\": \"f3\",\n    \"front\": \"What are the two steps involved in creating an object?\",\n    \"back\": \"1. **Declaration**: Declaring a variable of a class type (e.g., `String str;`). This creates a reference variable.\\n2. **Instantiation**: Using the `new` keyword to actually create the object and assign its memory address to the reference variable (e.g., `str = new String(\"Hello\");`). These are often combined into one line: `String str = new String(\"Hello\");`\"\n  },\n  {\n    \"id\": \"f4\",\n    \"front\": \"What happens if you declare an object variable but do not instantiate it with `new`?\",\n    \"back\": \"The reference variable will have a value of `null`, meaning it does not point to any object in memory. Attempting to use this variable to call a method (e.g., `obj.someMethod()`) will result in a `NullPointerException` at runtime.\"\n  },\n  {\n    \"id\": \"f5\",\n    \"front\": \"How do you declare and instantiate an object of a class named `Car` in a single statement?\",\n    \"back\": \"You use the following syntax: `Car myCar = new Car();` This declares a reference variable `myCar` of type `Car`, creates a new `Car` object, and assigns the reference of the new object to `myCar`.\"\n  }\n]\n```"
}