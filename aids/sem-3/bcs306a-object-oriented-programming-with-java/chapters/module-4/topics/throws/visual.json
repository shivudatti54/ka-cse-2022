{
  "type": "flowchart",
  "title": "Exception Propagation with 'throws' in Java",
  "description": "This flowchart illustrates how the 'throws' keyword propagates a checked exception from a method up the call stack until it is either caught by a try-catch block or causes the program to terminate.",
  "elements": [
    "Method A (throws Exception)",
    "Method B (calls A)",
    "Method C (calls B)",
    "JVM (calls C)"
  ],
  "relationships": [
    "A throws exception to B",
    "B propagates exception to C",
    "C propagates exception to JVM",
    "JVM terminates program if exception is never caught"
  ]
}