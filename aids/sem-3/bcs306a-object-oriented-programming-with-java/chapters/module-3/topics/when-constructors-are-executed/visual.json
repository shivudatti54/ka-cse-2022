{
  "type": "flowchart",
  "title": "Constructor Execution Order in Java Inheritance",
  "description": "A flowchart illustrating the sequence and rules for constructor execution when creating objects in a Java class hierarchy.",
  "elements": [
    "Object class (implicit)",
    "Grandparent class",
    "Parent class",
    "Child class",
    "`new` keyword invocation"
  ],
  "relationships": [
    "Child constructor call triggers a chain reaction up the inheritance hierarchy",
    "Each constructor implicitly calls `super()` as its first action, ensuring parent constructors run first",
    "The constructor of the ultimate base class, Object, runs first, followed by each subsequent child class down the chain"
  ]
}