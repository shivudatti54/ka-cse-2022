{
  "type": "flowchart",
  "title": "Thread Suspension Mechanisms in Java",
  "description": "A flowchart illustrating the deprecated suspend() method and the modern alternative using wait/notify for controlled thread suspension.",
  "elements": [
    "Main Thread",
    "Target Thread (to suspend)",
    "Object Monitor Lock",
    "wait() method call",
    "notify()/notifyAll() method call"
  ],
  "relationships": [
    "Main thread calls deprecated suspend() on Target Thread (UNSAFE)",
    "Main thread acquires lock and calls wait() on Target Thread (SAFE)",
    "Target Thread pauses execution and releases lock",
    "Main thread calls notify() to resume Target Thread"
  ]
}