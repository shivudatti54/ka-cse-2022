{
  "mnemonics": ["Keep Always New Threads", "Java Always New Tasks"],
  "keyPoints": [
    "The main thread is the primary thread in a Java application",
    "Creating a new thread is done using the Thread class",
    "The run method is called when a thread is started"
  ]
}
