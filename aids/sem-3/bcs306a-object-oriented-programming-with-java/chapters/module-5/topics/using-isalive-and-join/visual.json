{
  "type": "flowchart",
  "title": "Thread Lifecycle Check and Synchronization with isAlive() and join()",
  "description": "A flowchart demonstrating how a main thread can use isAlive() to check a child thread's status and join() to wait for its completion.",
  "elements": [
    "Main Thread",
    "Child Thread",
    "isAlive() Check",
    "join() Call",
    "Child Thread Completion"
  ],
  "relationships": [
    "Main Thread starts Child Thread",
    "Main Thread calls isAlive() on Child Thread (returns true/false)",
    "Main Thread calls join() on Child Thread (blocks until completion)",
    "Child Thread finishes execution"
  ]
}