{
  "type": "flowchart",
  "title": "Lifecycle of the Java Main Thread",
  "description": "A flowchart showing the creation, execution, and termination of the main thread in a Java application.",
  "elements": [
    "JVM Startup",
    "Main Thread Creation",
    "main() Method Execution",
    "Garbage Collection Thread (Optional)",
    "Thread Termination"
  ],
  "relationships": [
    "JVM Startup creates Main Thread",
    "Main Thread executes main() method",
    "main() method may spawn other threads (e.g., Garbage Collection)",
    "Main Thread terminates when main() method exits or System.exit() is called",
    "JVM shuts down when all non-daemon threads (including Main) terminate"
  ]
}