{
  "topicId": "lists",
  "examples": [
    {
      "id": "code-1",
      "language": "c",
      "title": "Singly Linked List - Node Structure and Creation",
      "description": "Defines the node structure for a singly linked list and function to create a new node",
      "code": "// Node structure definition\nstruct Node {\n    int data;\n    struct Node* next;\n};\n\n// Function to create a new node\nstruct Node* createNode(int data) {\n    struct Node* newNode = (struct Node*)malloc(sizeof(struct Node));\n    newNode->data = data;\n    newNode->next = NULL;\n    return newNode;\n}\n\n// Main function to demonstrate\nint main() {\n    struct Node* head = NULL;\n    head = createNode(10);\n    head->next = createNode(20);\n    head->next->next = createNode(30);\n    return 0;\n}",
      "explanation": "The Node structure contains two fields: data to store the element and next pointer to store address of next node. createNode() dynamically allocates memory and initializes the node.",
      "timeComplexity": "O(1) per node creation",
      "spaceComplexity": "O(1) per node (excluding data storage)"
    },
    {
      "id": "code-2",
      "language": "python",
      "title": "Singly Linked List - Insertion Operations",
      "description": "Python implementation of insertion operations at beginning, end, and middle of linked list",
      "code": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    \n    # Insert at beginning - O(1)\n    def insert_at_beginning(self, data):\n        new_node = Node(data)\n        new_node.next = self.head\n        self.head = new_node\n    \n    # Insert at end - O(n)\n    def insert_at_end(self, data):\n        new_node = Node(data)\n        if self.head is None:\n            self.head = new_node\n            return\n        current = self.head\n        while current.next:\n            current = current.next\n        current.next = new_node\n    \n    # Insert at position - O(n)\n    def insert_at_position(self, data, position):\n        new_node = Node(data)\n        if position == 0:\n            new_node.next = self.head\n            self.head = new_node\n            return\n        current = self.head\n        for _ in range(position - 1):\n            if current is None:\n                return\n            current = current.next\n        new_node.next = current.next\n        current.next = new_node\n    \n    # Traversal/Display\n    def display(self):\n        current = self.head\n        while current:\n            print(current.data, end=\" -> \")\n            current = current.next\n        print(\"None\")\n\n# Usage\nll = LinkedList()\nll.insert_at_beginning(10)\nll.insert_at_end(30)\nll.insert_at_position(20, 1)\nll.display()  # Output: 10 -> 20 -> 30 -> None",
      "explanation": "Insert at beginning updates head pointer. Insert at end requires traversal to find last node. Insert at position traverses to the (position-1)th node and updates pointers.",
      "timeComplexity": "O(1) for beginning, O(n) for end and position",
      "spaceComplexity": "O(1) for all operations"
    },
    {
      "id": "code-3",
      "language": "c",
      "title": "Singly Linked List - Deletion Operations",
      "description": "C implementation showing deletion of nodes from beginning, end, and by value",
      "code": "#include <stdio.h>\n#include <stdlib.h>\n\nstruct Node {\n    int data;\n    struct Node* next;\n};\n\n// Delete from beginning - O(1)\nstruct Node* delete_from_beginning(struct Node* head) {\n    if (head == NULL) return NULL;\n    struct Node* temp = head;\n    head = head->next;\n    free(temp);\n    return head;\n}\n\n// Delete from end - O(n)\nstruct Node* delete_from_end(struct Node* head) {\n    if (head == NULL) return NULL;\n    if (head->next == NULL) {\n        free(head);\n        return NULL;\n    }\n    struct Node* current = head;\n    while (current->next->next != NULL) {\n        current = current->next;\n    }\n    free(current->next);\n    current->next = NULL;\n    return head;\n}\n\n// Delete node with specific value - O(n)\nstruct Node* delete_by_value(struct Node* head, int value) {\n    if (head == NULL) return NULL;\n    if (head->data == value) {\n        struct Node* temp = head;\n        head = head->next;\n        free(temp);\n        return head;\n    }\n    struct Node* current = head;\n    while (current->next != NULL && current->next->data != value) {\n        current = current->next;\n    }\n    if (current->next != NULL) {\n        struct Node* temp = current->next;\n        current->next = temp->next;\n        free(temp);\n    }\n    return head;\n}\n\n// Display function\nvoid display(struct Node* head) {\n    struct Node* current = head;\n    while (current != NULL) {\n        printf(\"%d -> \", current->data);\n        current = current->next;\n    }\n    printf(\"NULL\\n\");\n}",
      "explanation": "Deletion requires updating pointers to bypass the node being deleted and then freeing the memory. For end deletion, we need to find the second-last node to update its next pointer to NULL.",
      "timeComplexity": "O(1) for beginning, O(n) for end and by value",
      "spaceComplexity": "O(1)"
    },
    {
      "id": "code-4",
      "language": "python",
      "title": "Doubly Linked List Implementation",
      "description": "Python implementation of a doubly linked list with bidirectional traversal",
      "code": "class DNode:\n    def __init__(self, data):\n        self.data = data\n        self.prev = None\n        self.next = None\n\nclass DoublyLinkedList:\n    def __init__(self):\n        self.head = None\n        self.tail = None\n    \n    # Insert at end - O(1) with tail pointer\n    def append(self, data):\n        new_node = DNode(data)\n        if self.tail is None:\n            self.head = self.tail = new_node\n        else:\n            new_node.prev = self.tail\n            self.tail.next = new_node\n            self.tail = new_node\n    \n    # Insert at beginning - O(1)\n    def prepend(self, data):\n        new_node = DNode(data)\n        if self.head is None:\n            self.head = self.tail = new_node\n        else:\n            new_node.next = self.head\n            self.head.prev = new_node\n            self.head = new_node\n    \n    # Delete from end - O(1) with tail\n    def delete_from_end(self):\n        if self.tail is None: return\n        if self.head == self.tail:\n            self.head = self.tail = None\n        else:\n            self.tail = self.tail.prev\n            self.tail.next = None\n    \n    # Forward traversal\n    def display_forward(self):\n        current = self.head\n        while current:\n            print(current.data, end=\" <-> \")\n            current = current.next\n        print(\"None\")\n    \n    # Backward traversal\n    def display_backward(self):\n        current = self.tail\n        while current:\n            print(current.data, end=\" <-> \")\n            current = current.prev\n        print(\"None\")\n\n# Usage\ndll = DoublyLinkedList()\ndll.append(10)\ndll.append(20)\ndll.append(30)\ndll.prepend(5)\ndll.display_forward()   # 5 <-> 10 <-> 20 <-> 30 <-> None\ndll.display_backward()  # 30 <-> 20 <-> 10 <-> 5 <-> None",
      "explanation": "Doubly linked list nodes have prev and next pointers, allowing traversal in both directions. Using tail pointer makes end operations O(1). The <-> symbol represents bidirectional links.",
      "timeComplexity": "O(1) for append/prepend/delete_end, O(n) for search",
      "spaceComplexity": "O(1) per operation"
    }
  ]
}