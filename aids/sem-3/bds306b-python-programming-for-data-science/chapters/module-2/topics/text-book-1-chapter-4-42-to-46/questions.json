{
  "questions": [
    {
      "question": "What is the purpose of the `map()` function in Python?",
      "type": "short_answer",
      "marks": 5,
      "hint": "It applies a given function to each item of an iterable (such as a list or tuple) and returns a list of the results."
    },
    {
      "question": "How does the `zip()` function differ from the `*` operator in Python?",
      "type": "long_answer",
      "marks": 10,
      "hint": "The `zip()` function takes iterables, aggregates them in a tuple, and returns it. The `*` operator unpacks an iterable and passes its elements as separate arguments to other functions."
    },
    {
      "question": "What is the difference between the `enumerate()` function and indexing in Python?",
      "type": "short_answer",
      "marks": 5,
      "hint": "The `enumerate()` function returns a tuple containing a count (from the start which defaults to 0) and the values obtained from iterating over the sequence. Indexing returns the element at a specific position."
    }
  ]
}
