{
  "type": "flowchart",
  "title": "Fundamentals of Binary Logic",
  "description": "A flowchart illustrating the core components of binary logic, how binary digits (bits) are processed through logic gates to produce outputs based on Boolean algebra.",
  "elements": [
    "Input Bits (0/1)",
    "Logic Gate (AND, OR, NOT, XOR, etc.)",
    "Output Bit (0/1)"
  ],
  "relationships": [
    "Input bits are fed into a logic gate",
    "The logic gate performs a Boolean operation on the input(s)",
    "The operation produces a single output bit"
  ]
}