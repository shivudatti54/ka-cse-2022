{
  "topicId": "sigcld-semantics",
  "diagrams": [
    {
      "id": "d-1",
      "title": "Process States and SIGCHLD",
      "description": "Shows the lifecycle of a child process and when SIGCHLD is sent",
      "elements": [
        "Running Child",
        "Stopped Child (SIGCHLD optional)",
        "Terminated Child (Zombie)",
        "Parent collects status via wait()",
        "SIGCHLD signal arrow",
        "init adoption for orphans"
      ],
      "purpose": "Understanding when SIGCHLD is delivered and zombie prevention"
    },
    {
      "id": "d-2",
      "title": "SIGCHLD Handler Flow",
      "description": "Flow diagram showing signal delivery and handling mechanism",
      "elements": [
        "Child termination",
        "Kernel sends SIGCHLD",
        "Signal handler invoked",
        "waitpid() loop",
        "Collect status",
        "Remove zombie entry"
      ],
      "purpose": "Understanding proper signal handler implementation"
    }
  ],
  "visuals": [
    {
      "id": "sigcld-semantics-svg",
      "title": "Sigcld Semantics",
      "description": "Visual diagram for sigcld semantics",
      "type": "animated-svg",
      "file": "assets/sigcld-semantics.svg",
      "svgContent": "<placeholder>",
      "animated": true
    }
  ]
}
