<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 600">
  <defs>
    <marker id="arrow" markerWidth="10" markerHeight="10" refX="9" refY="3" orient="auto">
      <path d="M0,0 L0,6 L9,3 z" fill="#1976d2"/>
    </marker>
  </defs>
  <rect width="800" height="600" fill="#f8f9fa"/>
  
  <g class="step" id="step0" data-narration="Welcome to polynomials of matrices. We'll explore how polynomials apply to square matrices, the Cayley-Hamilton theorem, and important practical applications.">
    <rect x="0" y="10" width="800" height="40" fill="#e3f2fd" stroke="#1976d2" stroke-width="2"/>
    <text x="400" y="38" text-anchor="middle" font-family="Arial" font-size="18" font-weight="bold" fill="#1565c0">Polynomials of Matrices</text>
  </g>
  
  <g class="step" id="step1" data-narration="A polynomial of a matrix substitutes the matrix into a scalar polynomial. For p(x) = a₀ + a₁x + a₂x² + ... + aₙxⁿ, we get p(A) = a₀I + a₁A + a₂A² + ... + aₙAⁿ.">
    <rect x="20" y="58" width="760" height="80" fill="#ffffff" stroke="#1976d2" stroke-width="1.5" rx="5"/>
    <text x="40" y="78" font-family="Arial" font-size="13" font-weight="bold" fill="#1976d2">1. Polynomial of a Matrix</text>
    <text x="40" y="96" font-family="Arial" font-size="11" fill="#333">Given p(x) = a₀ + a₁x + a₂x² + ... + aₙxⁿ and n×n matrix A:</text>
    <text x="40" y="114" font-family="Arial" font-size="12" font-style="italic" fill="#1565c0">p(A) = a₀I + a₁A + a₂A² + ... + aₙAⁿ</text>
    <text x="300" y="114" font-family="Arial" font-size="10" fill="#666">Example: p(x) = 2 + 3x - x² → p(A) = 2I + 3A - A²</text>
  </g>
  
  <g class="step" id="step2" data-narration="The characteristic polynomial of an n×n matrix A is p(λ) = det(A - λI). For 2×2 matrices, p(λ) = λ² - trace(A)λ + det(A).">
    <rect x="20" y="146" width="760" height="80" fill="#ffffff" stroke="#388e3c" stroke-width="1.5" rx="5"/>
    <text x="40" y="166" font-family="Arial" font-size="13" font-weight="bold" fill="#388e3c">2. Characteristic Polynomial</text>
    <text x="40" y="184" font-family="Arial" font-size="11" fill="#333">For n×n matrix A: p(λ) = det(A - λI) = (-1)ⁿλⁿ + cₙ₋₁λⁿ⁻¹ + ... + c₁λ + c₀</text>
    <text x="40" y="202" font-family="Arial" font-size="12" font-style="italic" fill="#2e7d32">For 2×2: p(λ) = λ² - trace(A)λ + det(A)</text>
    <text x="350" y="202" font-family="Arial" font-size="10" fill="#666">Example: A = [[3,1],[2,4]] → p(λ) = λ² - 7λ + 10</text>
  </g>
  
  <g class="step" id="step3" data-narration="The Cayley-Hamilton theorem states every square matrix satisfies its own characteristic equation: p(A) = 0. This fundamental result connects matrices to their polynomials.">
    <rect x="20" y="234" width="760" height="80" fill="#ffffff" stroke="#ff9800" stroke-width="1.5" rx="5"/>
    <text x="40" y="254" font-family="Arial" font-size="13" font-weight="bold" fill="#ff9800">3. Cayley-Hamilton Theorem</text>
    <text x="40" y="272" font-family="Arial" font-size="11" fill="#333">Every square matrix satisfies its own characteristic polynomial:</text>
    <text x="40" y="290" font-family="Arial" font-size="12" font-style="italic" fill="#e65100">p(A) = 0</text>
    <text x="100" y="290" font-family="Arial" font-size="10" fill="#666">If p(λ) = det(A - λI), then substituting A gives zero matrix</text>
    <text x="40" y="308" font-family="Arial" font-size="10" fill="#666">For 2×2: A² - trace(A)A + det(A)I = 0 → A² = trace(A)A - det(A)I</text>
  </g>
  
  <g class="step" id="step4" data-narration="The minimal polynomial is the monic polynomial of least degree that annihilates the matrix. It divides the characteristic polynomial and has the same distinct roots.">
    <rect x="20" y="322" width="760" height="91" fill="#ffffff" stroke="#9c27b0" stroke-width="1.5" rx="5"/>
    <text x="40" y="342" font-family="Arial" font-size="13" font-weight="bold" fill="#9c27b0">4. Minimal Polynomial</text>
    <text x="40" y="360" font-family="Arial" font-size="11" fill="#333">The minimal polynomial m(x) of matrix A is the monic polynomial of least degree such that m(A) = 0.</text>
    <text x="40" y="378" font-family="Arial" font-size="10" fill="#666">• It divides the characteristic polynomial: m(x) | p(x)</text>
    <text x="40" y="392" font-family="Arial" font-size="10" fill="#666">• They share the same distinct roots (eigenvalues)</text>
    <text x="40" y="406" font-family="Arial" font-size="10" fill="#666">• Degree of minimal polynomial ≤ n (matrix order), equals size of largest Jordan block</text>
  </g>
  
  <g class="step" id="step5" data-narration="Using Cayley-Hamilton, we can find matrix inverse. From p(A) = (-1)ⁿAⁿ + cₙ₋₁Aⁿ⁻¹ + ... + c₁A + c₀I = 0, rearrange to get A⁻¹ = -(1/c₀)[(-1)ⁿAⁿ⁻¹ + cₙ₋₁Aⁿ⁻² + ... + c₁I].">
    <rect x="20" y="421" width="760" height="80" fill="#ffffff" stroke="#f44336" stroke-width="1.5" rx="5"/>
    <text x="40" y="441" font-family="Arial" font-size="13" font-weight="bold" fill="#f44336">5. Finding Inverse Using Characteristic Polynomial</text>
    <text x="40" y="459" font-family="Arial" font-size="11" fill="#333">From p(A) = 0, rearrange for A⁻¹ (when det(A) ≠ 0):</text>
    <text x="40" y="477" font-family="Arial" font-size="11" font-style="italic" fill="#c62828">A⁻¹ = -(1/c₀)[(-1)ⁿAⁿ⁻¹ + cₙ₋₁Aⁿ⁻² + ... + c₁I]</text>
    <text x="40" y="495" font-family="Arial" font-size="10" fill="#666">Example: A = [[3,1],[2,2]], p(λ) = λ² - 5λ + 4</text>
  </g>
  
  <g class="step" id="step6" data-narration="CH theorem lets us compute high powers like A¹⁰⁰ by reducing them to combinations of I, A, A², ..., Aⁿ⁻¹. For 2×2, use A² = trace(A)A - det(A)I repeatedly.">
    <rect x="20" y="509" width="760" height="90" fill="#e8f5e9" stroke="#388e3c" stroke-width="1.5" rx="5"/>
    <text x="40" y="529" font-family="Arial" font-size="13" font-weight="bold" fill="#2e7d32">6. Computing Powers of Matrices</text>
    <text x="40" y="547" font-family="Arial" font-size="10" fill="#333">Express higher powers of A in terms of I, A, A², ..., Aⁿ⁻¹ using CH theorem:</text>
    <text x="40" y="563" font-family="Arial" font-size="10" fill="#666">For A = [[2,1],[1,2]], p(λ) = λ² - 4λ + 3</text>
    <text x="40" y="579" font-family="Arial" font-size="10" fill="#1565c0">A² = 4A - 3I, A³ = 13A - 12I, A⁴ = 40A - 39I</text>
    <text x="40" y="593" font-family="Arial" font-size="9" fill="#666">This method efficiently computes A¹⁰⁰ by reducing to linear combination of I and A</text>
  </g>
</svg>