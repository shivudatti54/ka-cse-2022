{
  "topicId": "big-data-characteristics-challenges",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Word Count MapReduce",
      "description": "Basic text processing algorithm",
      "code": "MAP(key, value):\n  For each word in value:\n    EMIT(word, 1)\n\nREDUCE(key, values):\n  sum = 0\n  For each v in values:\n    sum += v\n  EMIT(key, sum)",
      "explanation": "Mapper emits word counts, reducer sums them",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(k) where k = unique words"
    }
  ]
}