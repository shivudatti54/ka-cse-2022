{
  "topicId": "security-policies-models",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Access Control Check",
      "description": "Implements Bell-LaPadula simple security rule",
      "code": "FUNCTION checkAccess(userLevel, resourceLevel)\n  IF userLevel >= resourceLevel\n    RETURN ALLOW_READ\n  ELSE\n    RETURN DENY\nEND",
      "explanation": "Ensures users can only read resources at or below their clearance level",
      "timeComplexity": "O(1)",
      "spaceComplexity": "O(1)"
    }
  ]
}