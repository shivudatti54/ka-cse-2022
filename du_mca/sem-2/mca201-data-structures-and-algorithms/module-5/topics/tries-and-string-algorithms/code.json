{
  "topicId": "tries-and-string-algorithms",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "KMP Prefix Function",
      "description": "Compute longest prefix which is also suffix",
      "code": "function computeLPS(pattern):\n    lps = array of pattern.length\n    length = 0\n    i = 1\n    while i < pattern.length:\n        if pattern[i] == pattern[length]:\n            length++\n            lps[i] = length\n            i++\n        else:\n            ...",
      "explanation": "Builds failure function for efficient pattern jumps",
      "timeComplexity": "O(m)",
      "spaceComplexity": "O(m)"
    }
  ]
}