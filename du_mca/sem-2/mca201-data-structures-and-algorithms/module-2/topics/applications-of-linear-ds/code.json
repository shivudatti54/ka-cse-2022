{
  "topicId": "applications-of-linear-ds",
  "examples": [
    {
      "id": "code-1",
      "language": "Python",
      "title": "Queue using Two Stacks",
      "description": "Implements FIFO queue using LIFO stacks",
      "code": "class Queue:\n    def __init__(self):\n        self.in_stack = []\n        self.out_stack = []\n\n    def enqueue(self, item):\n        self.in_stack.append(item)\n\n    def dequeue(self):\n        if not self.out_stack:\n            while self.in_stack:\n                self.out_stack.append(self.in_stack.pop())\n        return self.out_stack.pop()",
      "explanation": "Enqueue to input stack, dequeue from output stack. Amortized O(1) operations",
      "timeComplexity": "O(1) amortized",
      "spaceComplexity": "O(n)"
    }
  ]
}