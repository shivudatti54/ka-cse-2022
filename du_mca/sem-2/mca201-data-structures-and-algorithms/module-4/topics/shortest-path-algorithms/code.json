{
  "topicId": "shortest-path-algorithms",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Dijkstra's Algorithm",
      "description": "Implementation using priority queue",
      "code": "function DIJKSTRA(graph, source):\n    dist[source] = 0\n    priority_queue = MinHeap()\n    while not priority_queue.empty():\n        u = priority_queue.extract_min()\n        for each neighbor v of u:\n            if dist[v] > dist[u] + weight(u, v):\n                dist[v] = dist[u] + weight(u, v)\n                priority_queue.decrease_key(v, dist[v])",
      "explanation": "Maintains minimum distances using priority queue, relaxes edges greedily",
      "timeComplexity": "O((V+E) log V)",
      "spaceComplexity": "O(V)"
    }
  ]
}