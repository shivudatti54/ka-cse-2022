{
  "topicId": "graph-representations-bfs-dfs",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "BFS Algorithm",
      "description": "Level-order traversal",
      "code": "BFS(Graph, start):\n    Queue q\n    visited[start] = True\n    q.enqueue(start)\n    while q not empty:\n        v = q.dequeue()\n        for neighbor in G[v]:\n            if not visited[neighbor]:\n                visited[neighbor] = True\n                q.enqueue(neighbor)",
      "explanation": "Processes nodes in layers using queue",
      "timeComplexity": "O(V+E)",
      "spaceComplexity": "O(V)"
    }
  ]
}