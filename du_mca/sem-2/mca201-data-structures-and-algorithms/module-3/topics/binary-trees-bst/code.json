{
  "topicId": "binary-trees-bst",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "BST Search",
      "description": "Iterative approach to find key in BST",
      "code": "function search(root, key):\n    while root \u2260 null:\n        if key == root.value:\n            return True\n        elif key < root.value:\n            root = root.left\n        else:\n            root = root.right\n    return False",
      "explanation": "Traverses left/right based on comparisons until key found or null reached",
      "timeComplexity": "O(h)",
      "spaceComplexity": "O(1)"
    },
    {
      "id": "code-2",
      "language": "Python",
      "title": "Inorder Traversal",
      "description": "Recursive implementation to print sorted values",
      "code": "def inorder(root):\n    if root:\n        inorder(root.left)\n        print(root.val)\n        inorder(root.right)",
      "explanation": "Visits left subtree, then root, then right subtree recursively",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(h)"
    }
  ]
}