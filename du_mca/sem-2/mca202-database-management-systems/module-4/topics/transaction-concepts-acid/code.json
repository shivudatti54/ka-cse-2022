{
  "topicId": "transaction-concepts-acid",
  "examples": [
    {
      "id": "code-1",
      "language": "SQL",
      "title": "Transaction with Rollback",
      "description": "Demonstrates atomicity through explicit transaction control",
      "code": "BEGIN TRANSACTION;\nUPDATE Accounts SET balance = balance - 100 WHERE id=1;\nUPDATE Accounts SET balance = balance + 100 WHERE id=2;\nCOMMIT;",
      "explanation": "Either both updates succeed (commit) or neither applies (rollback)",
      "timeComplexity": "O(1)",
      "spaceComplexity": "O(1)"
    }
  ]
}