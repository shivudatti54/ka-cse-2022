{
  "topicId": "indexing-and-hashing",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "B+ Tree Search",
      "description": "Algorithm to find a key in B+ tree",
      "code": "FUNCTION search(node, key):\n    WHILE node is not leaf:\n        FIND i such that key <= node.keys[i]\n        node = node.children[i]\n    RETURN position of key in leaf node",
      "explanation": "Traverses from root to leaf following key comparisons",
      "timeComplexity": "O(log n)",
      "spaceComplexity": "O(1)"
    }
  ]
}