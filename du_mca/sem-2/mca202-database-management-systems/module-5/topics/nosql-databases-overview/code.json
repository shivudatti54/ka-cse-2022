{
  "topicId": "nosql-databases-overview",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Vector Clock Conflict Resolution",
      "description": "Handling write conflicts in distributed systems",
      "code": "FUNCTION resolve_conflict(v1, v2):\n    IF v1 > v2: RETURN data1\n    ELSE IF v2 > v1: RETURN data2\n    ELSE: MERGE(data1, data2)",
      "explanation": "Uses version vectors to determine latest write",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(n)"
    }
  ]
}