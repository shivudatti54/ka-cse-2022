{
  "topicId": "combinational-circuits",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "4-bit Ripple Carry Adder",
      "description": "Demonstrates cascaded full-adders",
      "code": "FUNCTION RippleAdd(A[3:0], B[3:0])\n  CARRY = 0\n  FOR i FROM 0 TO 3\n    SUM[i], CARRY = FullAdder(A[i], B[i], CARRY)\n  RETURN SUM, CARRY",
      "explanation": "Each bit stage computes sum and carry sequentially",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}