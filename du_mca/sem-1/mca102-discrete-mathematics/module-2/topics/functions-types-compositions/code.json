{
  "topicId": "functions-types-compositions",
  "examples": [
    {
      "id": "code-1",
      "language": "Python",
      "title": "Bijective Check",
      "description": "Verifies if function is bijective over given domain",
      "code": "def is_bijective(domain, func):\n    outputs = [func(x) for x in domain]\n    return len(set(outputs)) == len(domain) == len(set(outputs))",
      "explanation": "Checks injectivity via unique outputs and surjectivity via coverage",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(n)"
    }
  ]
}