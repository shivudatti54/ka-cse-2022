{
  "topicId": "file-operations-in-c",
  "examples": [
    {
      "id": "code-1",
      "language": "C",
      "title": "File Copy Program",
      "description": "Demonstrates basic file operations with error checking",
      "code": "#include <stdio.h>\n\nint main() {\n    FILE *src = fopen(\"input.txt\", \"r\");\n    FILE *dest = fopen(\"output.txt\", \"w\");\n    \n    if (!src || !dest) {\n        perror(\"Error\");\n        return 1;\n    }\n    \n    int c;\n    while ((c = fgetc(src)) != EOF) {\n        fputc(c, dest);\n    }\n    \n    fclose(src);\n    fclose(dest);\n    return 0;\n}",
      "explanation": "Opens source in read mode, destination in write mode. Copies character by character.",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(1)"
    }
  ]
}