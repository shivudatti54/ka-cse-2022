{
  "topicId": "structures-unions-enumerations",
  "examples": [
    {
      "id": "code-1",
      "language": "C",
      "title": "Tagged Union Example",
      "description": "Type-safe union usage with enum discriminator",
      "code": "typedef enum { INT, FLOAT } Type;\ntypedef struct {\n  Type type;\n  union {\n    int i;\n    float f;\n  };\n} Value;",
      "explanation": "Uses enum to track active union member type",
      "timeComplexity": "O(1)",
      "spaceComplexity": "sizeof(Type) + max(sizeof(int), sizeof(float))"
    }
  ]
}