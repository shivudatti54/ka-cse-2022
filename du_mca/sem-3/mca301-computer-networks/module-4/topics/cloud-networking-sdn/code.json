{
  "topicId": "cloud-networking-sdn",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "SDN Packet Handling",
      "description": "Basic flow of OpenFlow packet processing",
      "code": "PROCESS_PACKET(packet):\n  CHECK flow_table FOR MATCH\n  IF match FOUND:\n    EXECUTE actions\n  ELSE:\n    SEND Packet-In TO controller\n    UPDATE flow_table WITH new rule",
      "explanation": "Shows how SDN switches handle unknown packets",
      "timeComplexity": "O(1) for table lookup",
      "spaceComplexity": "O(n) for flow entries"
    }
  ]
}