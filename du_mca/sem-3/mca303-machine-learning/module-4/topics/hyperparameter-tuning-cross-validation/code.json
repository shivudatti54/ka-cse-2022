{
  "topicId": "hyperparameter-tuning-cross-validation",
  "examples": [
    {
      "id": "code-1",
      "language": "Python",
      "title": "GridSearchCV Implementation",
      "description": "Hyperparameter tuning using scikit-learn's GridSearchCV",
      "code": "from sklearn.model_selection import GridSearchCV\nparams = {'C': [0.1, 1, 10], 'kernel': ['linear', 'rbf']}\ngrid = GridSearchCV(SVC(), param_grid=params, cv=5)\ngrid.fit(X_train, y_train)",
      "explanation": "Exhaustively searches specified parameter combinations with 5-fold CV",
      "timeComplexity": "O(n_params * n_cv * train_time)",
      "spaceComplexity": "O(n_params)"
    }
  ]
}