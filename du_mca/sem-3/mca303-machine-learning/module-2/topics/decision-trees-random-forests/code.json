{
  "topicId": "decision-trees-random-forests",
  "examples": [
    {
      "id": "code-1",
      "language": "python",
      "title": "Random Forest Classifier",
      "description": "Basic implementation using scikit-learn",
      "code": "from sklearn.ensemble import RandomForestClassifier\nrf = RandomForestClassifier(n_estimators=100, max_depth=5)\nrf.fit(X_train, y_train)",
      "explanation": "Initializes forest with 100 trees and depth constraint",
      "timeComplexity": "O(n_estimators * n_samples log n_samples)",
      "spaceComplexity": "O(n_estimators * n_nodes)"
    }
  ]
}