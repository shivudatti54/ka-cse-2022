{
  "topicId": "neural-networks-backpropagation",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Backpropagation Algorithm",
      "description": "Core weight update logic",
      "code": "ALGORITHM Backprop\nBEGIN\n  ForwardPass(inputs)\n  ComputeLoss(targets)\n  FOR layer IN reversed(network):\n    ComputeGradients(layer)\n    UpdateWeights(layer, learning_rate)\nEND",
      "explanation": "Iterates backward through layers, computing gradients via chain rule and applying updates",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(n)"
    }
  ]
}