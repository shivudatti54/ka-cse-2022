{
  "mnemonics": ["CLASS is a blueprint, OBJECT is the house built from it"],
  "keyPoints": [
    "A class is a user-defined data type that acts as a blueprint for creating objects",
    "An object is an instance of a class with its own state and behavior",
    "Classes encapsulate data (attributes) and functions (methods) into a single unit"
  ],
  "commonMistakes": [
    "Forgetting the semicolon after the class definition",
    "Confusing class (template) with object (instance)",
    "Using private class members directly without accessor methods"
  ]
}
