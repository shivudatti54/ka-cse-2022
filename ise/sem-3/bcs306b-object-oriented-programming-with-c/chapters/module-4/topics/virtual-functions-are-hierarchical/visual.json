{
  "type": "diagram",
  "title": "Hierarchical Virtual Function Invocation",
  "description": "A class hierarchy diagram showing how a virtual function call is resolved at runtime based on the actual object type, demonstrating polymorphic behavior.",
  "elements": [
    "Base Class: Shape (virtual draw())",
    "Derived Class: Circle (overrides draw())",
    "Derived Class: Rectangle (overrides draw())",
    "Client Code (calls draw() on Shape pointer)"
  ],
  "relationships": [
    "Base class declares a virtual function, derived classes provide specific implementations, and the client code invokes the function through a base class pointer, triggering the appropriate derived class method at runtime"
  ]
}
