{
  "type": "diagram",
  "title": "Memory Layout of an Array of Objects in C++",
  "description": "A visual representation of how multiple objects of a class are stored contiguously in memory when created as an array, highlighting the relationship between the array variable, individual objects, and their member data.",
  "elements": [
    "Array Variable (e.g., 'students[]')",
    "Individual Object (e.g., 'students[0]')",
    "Object's Member Data (e.g., 'studentID', 'name')",
    "Memory Addresses"
  ],
  "relationships": [
    "The array variable points to the first object in a contiguous block of memory",
    "Each subsequent object in the array follows immediately in memory",
    "Each object contains its own set of member data variables"
  ]
}
