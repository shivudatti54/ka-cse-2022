{
  "mnemonics": [
    "Father Always Keeps Energetic Records On My Desk",
    "Fat Albert Keeps Energetic Records On My Desktop"
  ],
  "keyPoints": [
    "File system hierarchy: / (root), /usr, /bin, /dev, /etc, /home, /lib, /lost+found, /media, /mnt, /opt, /proc, /root, /run, /sbin, /sys, /tmp, /usr/local, /var",
    "Directory structure: / (root), /bin, /boot, /dev, /etc, /home, /lib, /lost+found, /media, /mnt, /opt, /proc, /root, /run, /sbin, /sys, /tmp, /usr, /var",
    "File types: Regular files (files), directories, symbolic links, devices, special files (e.g. FIFOs), and sockets"
  ]
}
