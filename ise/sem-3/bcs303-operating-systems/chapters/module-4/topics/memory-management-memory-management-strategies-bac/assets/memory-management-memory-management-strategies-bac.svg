<svg width="420" height="400" data-topic-id="memory-management-strategies" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" viewBox="0 0 420 400" version="1.1">
    <defs>
        <linearGradient id="gradient" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%" stop-color="#7A68D8" stop-opacity="1"/>
            <stop offset="100%" stop-color="#6495ED" stop-opacity="1"/>
        </linearGradient>
        <linearGradient id="gradient2" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%" stop-color="#F5DEB3" stop-opacity="1"/>
            <stop offset="100%" stop-color="#FFA07A" stop-opacity="1"/>
        </linearGradient>
    </defs>
    <title>Memory Management Strategies</title>
    <rect x="10" y="10" width="400" height="370" fill="url(#gradient)"/>
    <g transform="translate(10, 10)">
        <rect x="0" y="0" width="100" height="100" fill="#6495ED" rx="10"/>
        <text x="50" y="50" fill="#FFFFFF" font-size="24" font-family="Arial" text-anchor="middle">Memory Hierarchy</text>
        <animateTransform attributeName="transform" type="translate" from="0 0" to="100 0" dur="5s" begin="0s" repeatCount="indefinite"/>
        <rect x="120" y="120" width="80" height="40" fill="#7A68D8" rx="10"/>
        <text x="220" y="170" fill="#FFFFFF" font-size="18" font-family="Arial" text-anchor="middle" data-narration="The memory hierarchy is a fundamental concept in memory management. It refers to the organization of memory into different levels, each with its own characteristics and access times."/>

        <ellipse x="140" y="260" width="100" height="40" fill="#6495ED" rx="10"/>
        <text x="220" y="310" fill="#FFFFFF" font-size="18" font-family="Arial" text-anchor="middle" data-narration="Level 1 (L1) Cache: A small, fast cache memory that stores frequently used data. Level 2 (L2) Cache: A larger cache memory that stores less frequently used data. Main Memory (RAM): The primary storage for data and program instructions. Main Frame (Storage): The permanent storage for data and programs."/>

        <rect x="10" y="380" width="200" height="20" fill="#7A68D8" rx="5"/>
        <text x="50" y="390" fill="#FFFFFF" font-size="16" font-family="Arial" text-anchor="middle" data-narration="Virtual Memory: A memory management technique that extends the physical memory (RAM) by temporarily storing data in secondary storage devices, such as hard drives or solid-state drives (SSDs). Types of Virtual Memory: Paging and Segmentation."/>

        <rect x="10" y="400" width="180" height="20" fill="#6495ED" rx="5"/>
        <text x="50" y="410" fill="#FFFFFF" font-size="16" font-family="Arial" text-anchor="middle" data-narration="Segmentation: A memory management strategy that divides the virtual address space into variable-size blocks called segments. Paging: A technique where the OS divides the virtual address space into fixed-size blocks called pages."/>

        <rect x="220" y="380" width="80" height="20" fill="#7A68D8" rx="5"/>
        <text x="310" y="390" fill="#FFFFFF" font-size="16" font-family="Arial" text-anchor="middle" data-narration="Segmentation Example: In Linux, each process is divided into different segments, such as code, data, and stack. Paging Example: In Windows, each process is divided into different pages, each with a unique identifier."/>

        <rect x="10" y="360" width="100" height="20" fill="#6495ED" rx="5"/>
        <text x="50" y="370" fill="#FFFFFF" font-size="16" font-family="Arial" text-anchor="middle" data-narration="Benefits of Memory Management Strategies: Efficient use of system resources, Prevention of memory leaks, and Ensuring that programs do not run out of memory."/>

        <rect x="10" y="400" width="360" height="20" fill="#F5DEB3" rx="5"/>
        <text x="50" y="410" fill="#FFFFFF" font-size="16" font-family="Arial" text-anchor="middle" data-narration="In conclusion, memory management strategies are essential for ensuring efficient use of system resources. By understanding the memory hierarchy, virtual memory, and memory management strategies, developers can create more efficient and scalable programs."/>

        <rect x="0" y="430" width="400" height="30" fill="#7A68D8" rx="5"/>
        <text x="50" y="440" fill="#FFFFFF" font-size="16" font-family="Arial" text-anchor="middle">Author: [Your Name]</text>
    </g>
</svg>