{
  "type": "flowchart",
  "title": "The Main Thread in Java Program Execution",
  "description": "A flowchart illustrating how the JVM creates and manages the main thread when a Java application starts, from program launch to method execution and termination.",
  "elements": [
    "Java Virtual Machine (JVM)",
    "main() Method",
    "Call Stack",
    "Garbage Collector Thread",
    "System.out.println()"
  ],
  "relationships": [
    "JVM creates main thread on program start",
    "main thread begins execution at main() method",
    "main thread pushes method calls onto its call stack",
    "main thread can spawn other threads",
    "main thread terminates when main() method completes"
  ]
}