{
  "content": "```json\n[\n  {\n    \"id\": \"sa1\",\n    \"question\": \"What is the primary purpose of defining a static method within a Java interface, and how does its accessibility differ from an abstract method in the same interface?\",\n    \"expectedPoints\": [\"Static methods in interfaces provide utility methods that are associated with the interface itself, not with any instance of a class that implements it.\", \"They are defined with the `static` keyword and contain a full method body.\", \"Unlike abstract methods, static methods are not inherited by implementing classes and can only be called directly on the interface name (e.g., InterfaceName.staticMethod()).\"],\n    \"marks\": 2\n  },\n  {\n    \"id\": \"sa2\",\n    \"question\": \"Explain the key differences between static and default methods in a Java interface. Your answer should cover their purpose, inheritance behavior, and how they are invoked.\",\n    \"expectedPoints\": [\"Purpose: Static methods are utility methods for the interface, while default methods provide a default implementation for implementing classes to use or override.\", \"Inheritance: Static methods are NOT inherited by implementing classes. Default methods ARE inherited and can be used or overridden by implementing classes.\", \"Invocation: Static methods are invoked directly on the interface (InterfaceName.method()). Default methods are invoked on an instance of a class that implements the interface (instance.method()).\", \"A class does not 'implement' or 'override' a static method from an interface; it defines its own unrelated static method if it uses the same signature.\"],\n    \"marks\": 5\n  },\n  {\n    \"id\": \"sa3\",\n    \"question\": \"A common design principle is to favor composition over inheritance. Discuss how the introduction of static and default methods in Java interfaces supports this principle. Provide a concrete example of how a static method in an interface could be used to create a factory for objects, eliminating the need for a separate factory class.\",\n    \"expectedPoints\": [\"Static and default methods allow interfaces to encapsulate more behavior, reducing the need to create abstract base classes just to share common code, thus promoting composition.\", \"An interface can now include its own factory methods (static) for creating instances, making separate factory classes often unnecessary.\", \"Example: An interface `Logger` could have a static method `Logger getFileLogger(String path)` that returns a concrete implementation (e.g., a `FileLogger`).\", \"This keeps the factory logic tightly coupled to the interface it produces, improving cohesion.\", \"Clients would create a logger via `Logger myLogger = Logger.getFileLogger(\"app.log\");` instead of `new FileLogger(\"app.log\")` or a separate `LoggerFactory`, making the code more dependent on the abstract interface than concrete classes.\"],\n    \"marks\": 10\n  }\n]\n```"
}
