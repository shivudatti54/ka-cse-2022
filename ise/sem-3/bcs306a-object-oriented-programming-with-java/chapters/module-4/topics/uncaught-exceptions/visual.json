{
  "type": "flowchart",
  "title": "Lifecycle of an Uncaught Exception in Java",
  "description": "A flowchart illustrating the path of an uncaught exception from its throw point to the JVM's default handler, resulting in program termination.",
  "elements": [
    "Method Call Stack",
    "Exception Thrown",
    "Current Method (looks for catch)",
    "Caller Method (looks for catch)",
    "JVM Default Handler",
    "Program Terminated"
  ],
  "relationships": [
    "Exception propagates up the call stack if not caught",
    "Each method in the stack has a chance to catch the exception",
    "If no catch block is found, the JVM handles it"
  ]
}
