{
  "mnemonics": ["DATA Pipeline", "ALU", "Registers"],
  "keyPoints": [
    "Data is processed in a pipeline: encoding, fetching, decoding, executing, storing",
    "Arithmetic Logic Unit (ALU) performs arithmetic and logical operations",
    "Registers hold data temporarily while it is being processed"
  ]
}
