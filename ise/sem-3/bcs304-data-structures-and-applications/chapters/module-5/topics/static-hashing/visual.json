{
  "type": "diagram",
  "title": "Static Hashing Structure and Collision Resolution",
  "description": "A visual breakdown of the static hashing data structure, including its components, the process of inserting data using a hash function, and methods for handling collisions.",
  "elements": [
    "Hash Table (Array of Buckets)",
    "Hash Function (h(k))",
    "Key-Value Pair (Record)",
    "Bucket (Slot/Index)",
    "Overflow Area (for separate chaining)"
  ],
  "relationships": [
    "Hash function maps a key to a bucket index",
    "Multiple keys hashing to the same index causes a collision",
    "Collisions are resolved using techniques like separate chaining (linked list) or open addressing (linear probing)"
  ]
}
