{
  "type": "flowchart",
  "title": "Contract Creation Transaction Flow",
  "description": "A flowchart illustrating the process and key components involved in a blockchain contract creation transaction, from initiation to deployment on the network.",
  "elements": [
    "User (EOA)",
    "Contract Creation Transaction (Zero-recipient tx)",
    "Bytecode & Constructor Arguments",
    "New Contract Address",
    "Deployed Smart Contract (on-chain)"
  ],
  "relationships": [
    "User sends a special transaction with no 'to' address but containing contract bytecode",
    "Network nodes process the transaction, executing the constructor code",
    "A new, unique contract address is deterministically generated",
    "The contract's runtime bytecode is stored at the new address on the blockchain"
  ]
}