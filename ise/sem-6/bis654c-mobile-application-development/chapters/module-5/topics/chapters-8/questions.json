{
  "questions": [
    {
      "question": "What is the primary purpose of the `open` method in SQLite database connection in Android?",
      "type": "short_answer",
      "marks": 5,
      "hint": "Optional hint"
    },
    {
      "question": "Write the correct Java code snippet to create a new SQLite database named 'mydatabase.db' in the context of an Android application?",
      "type": "code",
      "marks": 10,
      "hint": "Optional hint"
    },
    {
      "question": "What is the difference between `PRAGMA journal_mode = WAL;` and `PRAGMA journal_mode = TRUNCATE;` in SQLite?",
      "type": "long_answer",
      "marks": 15,
      "hint": "Optional hint"
    }
  ]
}
