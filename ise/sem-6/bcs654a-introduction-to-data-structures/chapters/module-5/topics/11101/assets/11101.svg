<svg width="420" height="400" data-topic-id="sorting-algorithms">
  <!-- Gradient background with pastel colors -->
  <defs>
    <linearGradient id="pastel-gradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#C7B8EA" stop-opacity="1" />
      <stop offset="50%" stop-color="#A7D2E8" stop-opacity="1" />
      <stop offset="100%" stop-color="#C5CAE9" stop-opacity="1" />
    </linearGradient>
  </defs>

  <!-- Title at top -->
  <text x="50%" y="20%" text-anchor="middle" font-size="24" fill="#2F4F7F">
    Sorting Algorithms
  </text>

  <!-- Section 1: Introduction to Sorting -->
  <rect x="20%" y="60%" width="300%" height="100" fill="url(#pastel-gradient)" data-narration="Sorting is the process of arranging elements in a specific order, usually in ascending or descending order, based on certain criteria. It is an essential operation in data structures and is used in various applications, such as database management, file management, and data analysis." />
  <animate transform="rotate(45 50% 50%)" begin="0s" dur="2s" fill="freeze" />
  <text x="50%" y="80%" text-anchor="middle" font-size="18" fill="#2F4F7F">
    What is Sorting?
  </text>
  <text x="50%" y="100%" text-anchor="middle" font-size="14" fill="#666">
    <tspan>Sorting is the process of arranging elements in a specific order, usually in ascending or descending order, based on certain criteria. It is an essential operation in data structures and is used in various applications, such as database management, file management, and data analysis.</tspan>
  </text>

  <!-- Section 2: Types of Sorting Algorithms -->
  <rect x="20%" y="200%" width="300%" height="100" fill="url(#pastel-gradient)" data-narration="There are several types of sorting algorithms, including comparison sorts, non-comparison sorts, and in-place sorts. Comparison sorts compare elements and swap them if they are in the wrong order. Non-comparison sorts use mathematical formulas to sort the data. In-place sorts sort the data in place, without requiring extra memory." />
  <animate transform="rotate(90 50% 50%)" begin="2s" dur="2s" fill="freeze" />
  <text x="50%" y="220%" text-anchor="middle" font-size="18" fill="#2F4F7F">
    Types of Sorting Algorithms
  </text>
  <text x="50%" y="240%" text-anchor="middle" font-size="14" fill="#666">
    <tspan>There are several types of sorting algorithms, including comparison sorts, non-comparison sorts, and in-place sorts. Comparison sorts compare elements and swap them if they are in the wrong order. Non-comparison sorts use mathematical formulas to sort the data. In-place sorts sort the data in place, without requiring extra memory.</tspan>
  </text>

  <!-- Section 3: Bubble Sort -->
  <rect x="20%" y="320%" width="300%" height="100" fill="url(#pastel-gradient)" data-narration="Bubble sort is a simple comparison sort that works by repeatedly swapping the adjacent elements if they are in the wrong order. It works by iterating through the array, comparing each pair of adjacent elements, and swapping them if they are in the wrong order. The process is repeated until the array is sorted." />
  <animate transform="rotate(135 50% 50%)" begin="4s" dur="2s" fill="freeze" />
  <text x="50%" y="340%" text-anchor="middle" font-size="18" fill="#2F4F7F">
    Bubble Sort
  </text>
  <text x="50%" y="360%" text-anchor="middle" font-size="14" fill="#666">
    <tspan>Bubble sort is a simple comparison sort that works by repeatedly swapping the adjacent elements if they are in the wrong order. It works by iterating through the array, comparing each pair of adjacent elements, and swapping them if they are in the wrong order. The process is repeated until the array is sorted.</tspan>
  </text>
  <line x1="50%" y1="370%" x2="60%" y2="370%" stroke="#2F4F7F" stroke-width="2" arrowhead="none" arrowmarker="url(#arrow-mark-2)" />
  <line x1="50%" y1="370%" x2="40%" y2="370%" stroke="#2F4F7F" stroke-width="2" arrowhead="none" arrowmarker="url(#arrow-mark-2)" />

  <!-- Section 4: Selection Sort -->
  <rect x="20%" y="440%" width="300%" height="100" fill="url(#pastel-gradient)" data-narration="Selection sort is a comparison sort that works by selecting the smallest (or largest) element from the unsorted part of the array and swapping it with the first element of the unsorted part. It works by repeatedly finding the smallest (or largest) element in the unsorted part of the array and moving it to the beginning (or end) of the unsorted part." />
  <animate transform="rotate(180 50% 50%)" begin="6s" dur="2s" fill="freeze" />
  <text x="50%" y="460%" text-anchor="middle" font-size="18" fill="#2F4F7F">
    Selection Sort
  </text>
  <text x="50%" y="480%" text-anchor="middle" font-size="14" fill="#666">
    <tspan>Selection sort is a comparison sort that works by selecting the smallest (or largest) element from the unsorted part of the array and swapping it with the first element of the unsorted part. It works by repeatedly finding the smallest (or largest) element in the unsorted part of the array and moving it to the beginning (or end) of the unsorted part.</tspan>
  </text>
  <line x1="50%" y1="500%" x2="60%" y2="500%" stroke="#2F4F7F" stroke-width="2" arrowhead="none" arrowmarker="url(#arrow-mark-2)" />
  <line x1="50%" y1="500%" x2="40%" y2="500%" stroke="#2F4F7F" stroke-width="2" arrowhead="none" arrowmarker="url(#arrow-mark-2)" />

  <!-- Footer -->
  <text x="50%" y="490%" text-anchor="middle" font-size="18" fill="#2F4F7F">
    End of Sorting Algorithms
  </text>
  <text x="50%" y="510%" text-anchor="middle" font-size="14" fill="#666">
    <tspan>Understanding sorting algorithms is crucial for efficient data management and analysis.</tspan>
  </text>
</svg>