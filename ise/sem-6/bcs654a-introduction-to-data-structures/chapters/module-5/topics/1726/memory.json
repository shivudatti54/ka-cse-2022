{
  "mnemonics": ["Sort by size", "Insert before"],
  "keyPoints": [
    "Bubble Sort uses a flag to track if any swaps were made in an iteration",
    "Insertion Sort compares the current element to the previous elements and shifts them if necessary",
    "Selection Sort finds the smallest (or largest) element and swaps it with the first element"
  ]
}
