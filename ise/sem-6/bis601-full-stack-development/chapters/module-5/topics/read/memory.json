{
  "mnemonics": [
    "CRUD - Create, Read, Update, Delete (Remember Read is the R)",
    "SELECT * FROM memory; - SQL analogy for reading all data"
  ],
  "keyPoints": [
    "Read operations retrieve data without modifying it",
    "APIs use GET requests for read operations",
    "Database queries use SELECT statements"
  ],
  "commonMistakes": [
    "Forgetting to handle null/empty results",
    "Not implementing proper error handling for database reads",
    "Failing to secure sensitive data during read operations"
  ]
}