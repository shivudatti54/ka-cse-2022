{
  "type": "flowchart",
  "title": "SQL Query Language Process Flow",
  "description": "A flowchart illustrating the flow of a SQL query from the frontend application through the backend server to the database and back.",
  "elements": [
    "Frontend Client (UI)",
    "Backend Server (Application Logic)",
    "Database Management System (DBMS)",
    "Database (Data Storage)"
  ],
  "relationships": [
    "Frontend sends a query request to Backend",
    "Backend constructs and sends a SQL query to DBMS",
    "DBMS executes the query on the Database and returns results",
    "Backend processes data and sends response to Frontend"
  ]
}
