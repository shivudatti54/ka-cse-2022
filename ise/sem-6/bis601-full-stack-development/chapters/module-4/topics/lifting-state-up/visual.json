{
  "type": "flowchart",
  "title": "Lifting State Up in React Component Hierarchy",
  "description": "A flowchart demonstrating how child components communicate by lifting shared state up to a common parent component.",
  "elements": ["Parent Component (State Holder)", "Child Component A", "Child Component B"],
  "relationships": [
    "Parent passes state down to Child A via props",
    "Parent passes state down to Child B via props",
    "Child A updates state by calling Parent's function via props",
    "Child B updates state by calling Parent's function via props"
  ]
}
