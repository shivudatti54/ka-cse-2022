<svg 
    width="420" 
    height="400" 
    viewBox="0 0 420 400" 
    xmlns="http://www.w3.org/2000/svg" 
    xmlns:xlink="http://www.w3.org/1999/xlink" 
    data-topic-id="parallel-computing-prime-numbers"
    data-slug="parallel-computing-prime-numbers"
    style="background: linear-gradient(to bottom, #5b68d8, #0097a7);">
    <title>Parallel Computing: Prime Numbers</title>
    <style>
        .section {
            margin-bottom: 20px;
        }
        .section[data-narration="introduction"] {
            background-color: #f9f9f9;
        }
        .section[data-narration="what-are-prime-numbers"] {
            background-color: #e6e6e6;
        }
        .section[data-narration="openmp-and-parallel-computing"] {
            background-color: #d9d9d9;
        }
        .section[data-narration="example-openmp-program"] {
            background-color: #ccc;
        }
        .section[data-narration="code"] {
            background-color: #b9b9b9;
        }
        .arrow {
            stroke: #007bff;
            stroke-width: 2;
            fill: none;
        }
    </style>

    <!-- Title and intro -->
    <rect 
        x="10" 
        y="10" 
        width="400" 
        height="50" 
        style="background-color: #5b68d8; border-radius: 10px 10px 0 0;" 
        data-narration="title">
    </rect>
    <rect 
        x="10" 
        y="60" 
        width="400" 
        height="100" 
        style="background-color: #f9f9f9; border-radius: 10px 10px 10px 10px;" 
        data-narration="introduction">
        <animate 
            attributeName="opacity" 
            dur="2s" 
            begin="0s" 
            fill="freeze"
            from="0" 
            to="1"
            repeatCount="indefinite">
        </animate>
    </rect>

    <!-- Section 1: What are Prime Numbers? -->
    <rect 
        x="10" 
        y="170" 
        width="400" 
        height="100" 
        style="background-color: #e6e6e6; border-radius: 10px 10px 10px 10px;" 
        data-narration="what-are-prime-numbers">
        <animate 
            attributeName="opacity" 
            dur="2s" 
            begin="0s" 
            fill="freeze"
            from="0" 
            to="1"
            repeatCount="indefinite">
        </animate>
        <text 
            x="20" 
            y="20" 
            font-size="24" 
            text-anchor="middle" 
            fill="#000">
            What are Prime Numbers?
        </text>
        <(text 
            x="20" 
            y="80" 
            font-size="18" 
            text-anchor="middle" 
            fill="#000">
            Prime numbers are natural numbers greater than 1 that have no positive divisors other than 1 and themselves.
        </text>
    </rect>

    <!-- Section 2: OpenMP and Parallel Computing -->
    <rect 
        x="10" 
        y="280" 
        width="400" 
        height="100" 
        style="background-color: #d9d9d9; border-radius: 10px 10px 10px 10px;" 
        data-narration="openmp-and-parallel-computing">
        <animate 
            attributeName="opacity" 
            dur="2s" 
            begin="0s" 
            fill="freeze"
            from="0" 
            to="1"
            repeatCount="indefinite">
        </animate>
        <text 
            x="20" 
            y="20" 
            font-size="24" 
            text-anchor="middle" 
            fill="#000">
            OpenMP and Parallel Computing
        </text>
        <text 
            x="20" 
            y="80" 
            font-size="18" 
            text-anchor="middle" 
            fill="#000">
            OpenMP provides a set of directives that allow us to specify which parts of the program should be executed in parallel.
        </text>
    </rect>

    <!-- Section 3: Example OpenMP Program -->
    <rect 
        x="10" 
        y="380" 
        width="400" 
        height="100" 
        style="background-color: #ccc; border-radius: 10px 10px 10px 10px;" 
        data-narration="example-openmp-program">
        <animate 
            attributeName="opacity" 
            dur="2s" 
            begin="0s" 
            fill="freeze"
            from="0" 
            to="1"
            repeatCount="indefinite">
        </animate>
        <rect 
            x="20" 
            y="20" 
            width="360" 
            height="60" 
            style="background-color: #f0f0f0; border-radius: 10px 10px 10px 10px;" 
            data-narration="example-openmp-program-code">
            <animate 
                attributeName="opacity" 
                dur="2s" 
                begin="0s" 
                fill="freeze"
                from="0" 
                to="1"
                repeatCount="indefinite">
            </animate>
            <text 
                x="20" 
                y="40" 
                font-size="18" 
                text-anchor="middle" 
                fill="#000">
            ```
            #pragma omp parallel for
            for (int i = 0; i < n; i++) {
                int taskId
            }
            </text>
        </rect>
        <text 
            x="20" 
            y="100" 
            font-size="18" 
            text-anchor="middle" 
            fill="#000">
            Example OpenMP Program
        </text>
    </rect>

    <!-- Section 4: Code -->
    <rect 
        x="10" 
        y="480" 
        width="400" 
        height="100" 
        style="background-color: #b9b9b9; border-radius: 10px 10px 10px 10px;" 
        data-narration="code">
        <animate 
            attributeName="opacity" 
            dur="2s" 
            begin="0s" 
            fill="freeze"
            from="0" 
            to="1"
            repeatCount="indefinite">
        </animate>
        <pre 
            x="20" 
            y="20" 
            font-size="18" 
            text-anchor="middle" 
            fill="#000">
            ```c
            // PrimeNumbersOpenMP.c
            #include <stdio.h>
            #include <omp.h>

            // Function to check if a number is prime
            int isPrime(int num) {
                if (num <= 1) {
                    return 0;
                }
                for (int i = 2; i * i <= num; i++) {
                    if (num % i == 0) {
                        return 0;
                    }
                }
                return 1;
            }

            int main(int argc, char ** argv) {
                int n;
                if (argc != 2) {
                    printf("Usage: %s <n>\n", argv[0]);
                    return 1;
                }
                n = atoi(argv[1]);

                // Create a task array to store the number of iterations for each thread
                int taskSize = n / omp_get_max_threads();
                int *tasks = (int *)malloc(n * sizeof(int));

                // Initialize the task array
                for (int i = 0; i < n; i++) {
                    tasks[i] = 1;
                }

                // Parallelize the loop
                #pragma omp parallel for
                for (int i = 0; i < n; i++) {
                    // Assign task to the current thread
                    int taskId
                }
            }
            ```
        </pre>
    </rect>

    <!-- Footer -->
    <rect 
        x="10" 
        y="580" 
        width="400" 
        height="50" 
        style="background-color: #5b68d8; border-radius: 10px 10px 0 0;" 
        data-narration="footer">
    </rect>
    <path 
        d="M 10 10 L 10 100 L 400 100 L 400 0 L 10 0" 
        stroke="#007bff" 
        stroke-width="2" 
        fill="none">
    </path>
    <path 
        d="M 10 10 L 20 20" 
        stroke="#007bff" 
        stroke-width="2" 
        fill="none" 
        class="arrow">
    </path>
</svg>