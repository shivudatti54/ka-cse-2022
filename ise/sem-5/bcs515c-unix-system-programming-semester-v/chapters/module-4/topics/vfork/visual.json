{
  "type": "flowchart",
  "title": "vfork() Process Creation Flow",
  "description": "A flowchart illustrating the execution flow and memory behavior of the vfork() system call compared to a traditional fork().",
  "elements": [
    "Parent Process (Before vfork)",
    "vfork() System Call",
    "Child Process Execution",
    "Parent Process (Suspended)",
    "_exit() or exec() Call",
    "Parent Process (Resumed)"
  ],
  "relationships": [
    "Parent calls vfork() to create child",
    "Child runs in parent's address space, parent is suspended",
    "Child must use _exit() or exec() to terminate",
    "Parent resumes execution after child terminates"
  ]
}
