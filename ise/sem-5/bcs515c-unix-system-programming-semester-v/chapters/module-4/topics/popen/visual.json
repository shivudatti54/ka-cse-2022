{
  "type": "flowchart",
  "title": "Process Flow of popen() Function",
  "description": "Illustrates the creation of a pipe and a child process to execute a shell command and establish a unidirectional data stream.",
  "elements": [
    "Parent Process",
    "popen() call",
    "Kernel (pipe creation)",
    "Child Process",
    "Shell (/bin/sh)",
    "Command Execution"
  ],
  "relationships": [
    "Parent calls popen() -> Kernel creates a pipe and forks a child",
    "Child process dup2() to redirect stdin/stdout -> Executes shell",
    "Shell executes the command -> Data flows through the pipe back to parent"
  ]
}
