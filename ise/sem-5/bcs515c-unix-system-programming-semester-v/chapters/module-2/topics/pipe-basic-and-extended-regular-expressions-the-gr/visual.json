{
  "type": "flowchart",
  "title": "Data Flow using UNIX Pipe and grep with Regular Expressions",
  "description": "A flowchart illustrating how the pipe operator connects a command's output to grep, which then filters data using basic (BRE) or extended (ERE) regular expressions.",
  "elements": [
    "Command 1 (e.g., cat, ls)",
    "Pipe Operator (|)",
    "grep Command",
    "Pattern (BRE/ERE)",
    "Filtered Output"
  ],
  "relationships": [
    "Command 1 stdout flows into Pipe",
    "Pipe directs data as stdin to grep",
    "grep applies Regular Expression pattern to filter input",
    "grep sends matching lines to stdout"
  ]
}
