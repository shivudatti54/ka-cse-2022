{
  "type": "flowchart",
  "title": "Process Flow for Creating a Directory with mkdir()",
  "description": "A flowchart showing the steps and system calls involved when a user program calls the mkdir() function to create a new directory in the UNIX filesystem.",
  "elements": [
    "User Program",
    "mkdir() Library Function",
    "Kernel System Call Handler",
    "Virtual File System (VFS)",
    "Underlying Filesystem (e.g., ext4)",
    "Parent Directory Inode",
    "New Directory Inode",
    "Disk Blocks"
  ],
  "relationships": [
    "Program calls mkdir() function",
    "Library function invokes system call",
    "Kernel handler processes request",
    "VFS routes call to specific filesystem",
    "Filesystem checks permissions on parent",
    "Filesystem allocates new inode and data blocks",
    "Kernel updates caches and returns status"
  ]
}
