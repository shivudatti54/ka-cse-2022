{
  "type": "flowchart",
  "title": "Process of the UNIX `type` Command",
  "description": "A flowchart illustrating the decision-making process the `type` command uses to identify and report the nature of a given command.",
  "elements": [
    "User Input (command name)",
    "Check for Aliases",
    "Check for Shell Keywords",
    "Check for Shell Builtins",
    "Search $PATH for Executable File",
    "Output Result"
  ],
  "relationships": [
    "Process starts with user input, then checks each command type in a specific order (alias -> keyword -> builtin -> file) until a match is found, then outputs the result and exits."
  ]
}
