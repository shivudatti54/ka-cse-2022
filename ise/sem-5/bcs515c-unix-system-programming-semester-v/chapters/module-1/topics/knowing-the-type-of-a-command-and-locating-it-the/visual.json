{
  "type": "flowchart",
  "title": "UNIX Command Type Identification and Location Flow",
  "description": "A flowchart illustrating the process a shell uses to determine the type of a command (built-in, alias, external) and locate its executable file, with a note on root login privileges.",
  "elements": [
    "User enters command",
    "Shell checks for alias",
    "Shell checks for built-in",
    "PATH environment variable",
    "which/type/whereis commands",
    "/usr/bin, /bin, /sbin directories",
    "Root login (superuser privileges)"
  ],
  "relationships": [
    "Process flow from user input to command execution",
    "Hierarchical command resolution (alias -> built-in -> external)",
    "PATH directories are searched in order for external commands",
    "Root login can access privileged directories like /sbin"
  ]
}
