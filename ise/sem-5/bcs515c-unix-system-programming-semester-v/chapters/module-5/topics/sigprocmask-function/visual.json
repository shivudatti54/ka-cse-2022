{
  "type": "flowchart",
  "title": "Process Signal Mask Management with sigprocmask()",
  "description": "A flowchart illustrating how a process uses the sigprocmask function to block and unblock signals, preventing them from being delivered and handled immediately.",
  "elements": [
    "Process with Current Signal Mask",
    "sigprocmask() Function Call",
    "New Signal Mask (how argument)",
    "Old Signal Mask (optional output)",
    "Kernel Signal Dispatcher",
    "Pending Signals Set",
    "Signal Handler"
  ],
  "relationships": [
    "Process calls sigprocmask() to change its mask, the kernel updates the process's mask and uses it to block signals, which are then added to a pending set and only delivered when unblocked"
  ]
}
