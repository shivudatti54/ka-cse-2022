{
  "type": "flowchart",
  "title": "Flow of the abort() Function",
  "description": "A flowchart illustrating the sequence of actions performed by the abort() function in UNIX system programming, leading to process termination.",
  "elements": [
    "Process calls abort()",
    "SIGABRT signal is raised",
    "Signal handler is called (if set)",
    "Process terminates",
    "Core dump file is generated (optional)"
  ],
  "relationships": [
    "Function call triggers signal",
    "Signal delivery invokes handler",
    "Handler execution completes or is bypassed",
    "Termination follows signal handling",
    "Core dump creation depends on ulimit settings"
  ]
}
