{
  "type": "flowchart",
  "title": "The `system()` Function Execution Flow",
  "description": "A flowchart illustrating the process of how the `system()` function creates a shell to execute a command in a UNIX environment.",
  "elements": [
    "Calling Program",
    "system() function",
    "/bin/sh shell",
    "fork() syscall",
    "exec() syscall",
    "Command Execution",
    "Exit Status"
  ],
  "relationships": [
    "Program calls system() with command string",
    "system() invokes /bin/sh",
    "shell uses fork() to create child process",
    "child process uses exec() to run command",
    "exit status is returned to system()",
    "system() returns exit status to calling program"
  ]
}
