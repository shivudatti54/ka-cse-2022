{
  "type": "diagram",
  "title": "UNIX Filesystem Hierarchy and System Call Relationships",
  "description": "A visual representation of the core UNIX file-related data structures and the system calls that operate on them.",
  "elements": [
    "Process (fd table)",
    "File Descriptor (fd)",
    "File Table (global kernel struct)",
    "Inode Table (v-node)",
    "Directory (special file)",
    "Disk Blocks"
  ],
  "relationships": [
    "open() creates link from fd to file table entry",
    "read()/write() use fd to access inode data",
    "link() creates new directory entry pointing to same inode",
    "unlink() removes directory entry, inode freed if count=0"
  ]
}
