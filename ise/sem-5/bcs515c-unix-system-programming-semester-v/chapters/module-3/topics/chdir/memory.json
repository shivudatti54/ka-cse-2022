{
  "mnemonics": ["Change Directory, like 'cd' in shell", "CHange DIRectory = 'chdir'"],
  "keyPoints": [
    "Changes the current working directory of the calling process",
    "Takes a single argument: the path to the new directory",
    "Returns 0 on success and -1 on failure"
  ],
  "commonMistakes": [
    "Not checking the return value for errors",
    "Using relative paths without considering the current working directory",
    "Assuming it affects the shell's current directory (it only affects the process)"
  ]
}
