{
  "mnemonics": ["Linux users", "Linux groups"],
  "keyPoints": [
    "To match exactly 5 'a's, use \\a{5} in regex",
    "To match 0 or more 'a's, use \\a*",
    "To match at least 3 'a's, use \\a{3,}",
    "To match a single 'a' followed by a 'b' and ending with a 'c', use \\a\\b\\c",
    "To find all files with the word 'test' in them, use find . -regex 'test'"
  ]
}
