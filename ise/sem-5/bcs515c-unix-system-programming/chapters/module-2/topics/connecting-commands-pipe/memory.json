{
  "mnemonics": ["Pipes are like water pipes, they connect commands to each other"],
  "keyPoints": [
    "Pipe (- | > >> 2>> ) : Connects output of one command to input of another command",
    "Standard output of first command is input to second command",
    "Avoid using pipe for large files, as it can cause performance issues"
  ]
}
