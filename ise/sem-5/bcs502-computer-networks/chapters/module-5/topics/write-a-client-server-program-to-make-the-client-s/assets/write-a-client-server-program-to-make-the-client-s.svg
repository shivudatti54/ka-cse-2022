<svg width="420" height="400" data-topic-id="client-server-program-for-file-sharing">
  <!-- Gradient background with pastel colors -->
  <defs>
    <linearGradient id="gradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#C7B8EA" stop-opacity="1"/>
      <stop offset="100%" stop-color="#87CEEB" stop-opacity="1"/>
    </linearGradient>
  </defs>
  <rect x="10" y="10" width="400" height="380" fill="url(#gradient)"/>
  <!-- Title at top -->
  <text x="20" y="20" font-size="24" font-family="Arial" fill="#3498DB">
    Client-Server Program for File Sharing
  </text>
  <!-- Section 1: Introduction -->
  <section data-narration="Introduction">
    <rect x="10" y="60" width="400" height="50" fill="#F9F9F9" rx="10">
      <animateTransform attributeName="transform" type="translate" from="0 0" to="0 30" dur="1s" begin="-0.5s" repeatCount="indefinite"/>
    </rect>
    <text x="20" y="70" font-size="18" font-family="Arial" fill="#333">
      In computer networks, client-server architecture is a widely used model where a client machine requests services from a server machine.
    </text>
  </section>
  <!-- Section 2: Client-Server Architecture -->
  <section data-narration="Client-Server Architecture">
    <rect x="10" y="120" width="400" height="50" fill="#F9F9F9" rx="10">
      <animateTransform attributeName="transform" type="translate" from="0 0" to="0 60" dur="1s" begin="-0.5s" repeatCount="indefinite"/>
    </rect>
    <text x="20" y="70" font-size="18" font-family="Arial" fill="#333">
    A client-server architecture consists of two types of machines: client and server.
  </text>
  </section>
  <!-- Section 3: Key Components -->
  <section data-narration="Key Components">
    <rect x="10" y="180" width="400" height="50" fill="#F9F9F9" rx="10">
      <animateTransform attributeName="transform" type="translate" from="0 0" to="0 90" dur="1s" begin="-0.5s" repeatCount="indefinite"/>
    </rect>
    <text x="20" y="100" font-size="18" font-family="Arial" fill="#333">
    Key Components:
      <ul>
        <li>Sockets: A socket is a endpoint for communication between two devices (client and server) in a network.</li>
        <li>TCP/IP: TCP/IP (Transmission Control Protocol/Internet Protocol) is a suite of protocols used for communication over the internet.</li>
      </ul>
    </text>
  </section>
  <!-- Section 4: Client-Server Program for File Sharing -->
  <section data-narration="Client-Server Program for File Sharing">
    <rect x="10" y="240" width="400" height="50" fill="#F9F9F9" rx="10">
      <animateTransform attributeName="transform" type="translate" from="0 0" to="0 120" dur="1s" begin="-0.5s" repeatCount="indefinite"/>
    </rect>
    <text x="20" y="130" font-size="18" font-family="Arial" fill="#333">
    To create a client-server program for file sharing, we will use Python programming language.
  </text>
  </section>
  <!-- Section 5: Server Code -->
  <section data-narration="Server Code">
    <rect x="10" y="300" width="400" height="50" fill="#F9F9F9" rx="10">
      <animateTransform attributeName="transform" type="translate" from="0 0" to="0 150" dur="1s" begin="-0.5s" repeatCount="indefinite"/>
    </rect>
    <text x="20" y="210" font-size="18" font-family="Arial" fill="#333">
    <pre>
      import socket
      import os

      def start_server():
        # Create a socket object
        server_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

        # Bind the socket to a specific IP address and port number
        server_socket.bind(('127.0.0.1', 12345))

        # Listen for incoming connections
        server_socket.listen(1)

        print("Server is listening for incoming connections...")

        # Accept incoming connections
        client_socket, address = server_socket.accept()

        print(f"Connection from {address} has been established.")

        # Receive file name from client
        file_name = client_socket.recv(1024).decode('utf-8')

        print(f"Received file name: {file_name}")
    </pre>
  </text>
  </section>
  <!-- Section 6: Client Code -->
  <section data-narration="Client Code">
    <rect x="10" y="360" width="400" height="50" fill="#F9F9F9" rx="10">
      <animateTransform attributeName="transform" type="translate" from="0 0" to="0 180" dur="1s" begin="-0.5s" repeatCount="indefinite"/>
    </rect>
    <text x="20" y="270" font-size="18" font-family="Arial" fill="#333">
    <pre>
      import socket

      def start_client():
        # Create a socket object
        client_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)

        # Connect to the server
        client_socket.connect(('127.0.0.1', 12345))

        # Send file name to server
        file_name = input("Enter file name: ")
        client_socket.send(file_name.encode('utf-8'))

        # Receive file from server
        file = client_socket.recv(1024)
        print(f"Received file: {file}")
    </pre>
  </text>
  </section>
  <!-- Footer -->
  <text x="20" y="400" font-size="18" font-family="Arial" fill="#666">
    Created by [Your Name]
  </text>
</svg>