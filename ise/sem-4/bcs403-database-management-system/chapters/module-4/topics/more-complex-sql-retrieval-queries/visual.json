{
  "type": "flowchart",
  "title": "Complex SQL Query Processing Flow",
  "description": "A flowchart illustrating the logical processing order and key components of a complex SQL SELECT query",
  "elements": [
    "SELECT clause",
    "FROM clause",
    "WHERE clause",
    "GROUP BY clause",
    "HAVING clause",
    "ORDER BY clause",
    "Result Set"
  ],
  "relationships": [
    "FROM identifies source tables first, then WHERE filters rows, GROUP BY aggregates data, HAVING filters groups, SELECT processes columns, and ORDER BY sorts final results"
  ]
}
