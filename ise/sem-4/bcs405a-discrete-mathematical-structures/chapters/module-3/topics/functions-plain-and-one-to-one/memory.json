{
  "mnemonics": ["Many Input, Many Output (MIMO)", "One Input, One Output (IO)"],
  "keyPoints": [
    "A function is a relation where every input has exactly one output.",
    "A function is one-to-one (injective) if no two different inputs have the same output.",
    "A function is onto (surjective) if every output is covered by the outputs of the function."
  ]
}
