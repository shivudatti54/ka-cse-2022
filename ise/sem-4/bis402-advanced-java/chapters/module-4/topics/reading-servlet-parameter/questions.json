{
  "questions": [
    {
      "question": "Write a Java method that reads a servlet parameter 'name' from the HTTP request and returns it as a string. The request parameter name is 'username'. What would be the return type of the following method: public String getUserName(HttpServletRequest request) { return request.getParameter('username');}",
      "type": "long_answer",
      "marks": 10,
      "hint": "Optional hint for long answer questions"
    },
    {
      "question": "Can the following code read a servlet parameter 'age' from the HTTP request: getBean(request.getParameter('age')) ? If not, explain why.",
      "type": "short_answer",
      "marks": 5,
      "hint": "Optional hint for short answer questions"
    },
    {
      "question": "How can you check if a servlet parameter 'country' exists in the HTTP request before trying to read it? Provide a Java code snippet to illustrate this.",
      "type": "short_answer",
      "marks": 5,
      "hint": "Optional hint for short answer questions"
    }
  ]
}
