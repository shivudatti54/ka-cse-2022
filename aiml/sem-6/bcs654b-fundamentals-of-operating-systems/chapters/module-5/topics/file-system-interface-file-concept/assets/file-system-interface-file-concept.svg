<svg width="420" height="400" data-topic-id="file-system-interface-file-concept">
  <!-- Gradient background -->
  <defs>
    <linearGradient id="pastel-gradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#C9E4CA" stop-opacity="1"/>
      <stop offset="100%" stop-color="#D9F3F5" stop-opacity="1"/>
    </linearGradient>
  </defs>
  <rect x="0" y="0" width="420" height="400" fill="url(#pastel-gradient)"/>

  <!-- Title -->
  <text x="10" y="20" font-size="24" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
    File System Interface: File Concept
  </text>

  <!-- Section 1: Introduction -->
  <section id="section-1" data-narration="Introduction to Files">
    <rect x="10" y="50" width="400" height="100" fill="#FFFFFF" rx="10"/>
    <text x="15" y="70" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      A file is a collection of related data stored on a storage device.
    </text>
    <animateTransform attribute="x" type="rotate" from="0" to="360" dur="2000" repeatCount="indefinite"/>
  </section>

  <!-- Section 2: File Structure -->
  <section id="section-2" data-narration="File Structure">
    <rect x="10" y="170" width="400" height="100" fill="#FFFFFF" rx="10"/>
    <text x="15" y="190" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      A file consists of several components:
      <br>File Name: The name given to the file.
      <br>File Type: The type of data stored in the file.
      <br>File Size: The total amount of data stored in the file.
      <br>File Location: The location of the file on the storage device.
    </text>
    <text x="15" y="220" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      <tspan>File Name: The name given to the file, which is used to identify it.</tspan>
      <tspan>File Type: The type of data stored in the file, such as text or image.</tspan>
      <tspan>File Size: The total amount of data stored in the file.</tspan>
      <tspan>File Location: The location of the file on the storage device.</tspan>
    </text>
  </section>

  <!-- Section 3: File Operations -->
  <section id="section-3" data-narration="File Operations">
    <rect x="10" y="310" width="400" height="100" fill="#FFFFFF" rx="10"/>
    <text x="15" y="330" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      Files can be manipulated using various operations, including:
      <br>Create: Creating a new file on the storage device.
      <br>Delete: Deleting an existing file from the storage device.
      <br>Read: Accessing the data stored in a file.
      <br>Write: Modifying or adding data to a file.
      <br>Copy: Creating a duplicate copy of a file.
      <br>Move: Reorganizing the location of a file on the storage device.
    </text>
    <text x="15" y="360" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      <tspan>Create: Creating a new file on the storage device.</tspan>
      <tspan>Delete: Deleting an existing file from the storage device.</tspan>
      <tspan>Read: Accessing the data stored in a file.</tspan>
      <tspan>Write: Modifying or adding data to a file.</tspan>
      <tspan>Copy: Creating a duplicate copy of a file.</tspan>
      <tspan>Move: Reorganizing the location of a file on the storage device.</tspan>
    </text>
  </section>

  <!-- Section 4: File Attributes -->
  <section id="section-4" data-narration="File Attributes">
    <rect x="10" y="410" width="400" height="100" fill="#FFFFFF" rx="10"/>
    <text x="15" y="430" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      Files can have various attributes that affect their behavior and usage:
      <br>File permissions: The access rights granted to users or groups to read, write, or execute a file.
      <br>File ownership: The user or group responsible for managing a file.
      <br>File timestamps: The dates and times associated with a file's creation, modification, and access.
    </text>
    <text x="15" y="460" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      <tspan>File permissions: The access rights granted to users or groups to read, write, or execute a file.</tspan>
      <tspan>File ownership: The user or group responsible for managing a file.</tspan>
      <tspan>File timestamps: The dates and times associated with a file's creation, modification, and access.</tspan>
    </text>
  </section>

  <!-- Section 5: File System Interface -->
  <section id="section-5" data-narration="File System Interface">
    <rect x="10" y="520" width="400" height="100" fill="#FFFFFF" rx="10"/>
    <text x="15" y="540" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      The file system interface provides a layer of abstraction between the operating system and the storage device.
    </text>
    <text x="15" y="570" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      <tspan>The file system interface provides a layer of abstraction between the operating system and the storage device.</tspan>
    </text>
  </section>

  <!-- Section 6: Conclusion -->
  <section id="section-6" data-narration="Conclusion">
    <rect x="10" y="620" width="400" height="100" fill="#FFFFFF" rx="10"/>
    <text x="15" y="640" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      In conclusion, files are a fundamental component of a file system interface.
    </text>
    <text x="15" y="670" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
      <tspan>In conclusion, files are a fundamental component of a file system interface.</tspan>
    </text>
  </section>

  <!-- Footer -->
  <text x="10" y="390" font-size="18" font-family="Arial, sans-serif" text-anchor="start" fill="#2E4053">
    Created with <a href="#">SVG Editor</a>
  </text>
</svg>