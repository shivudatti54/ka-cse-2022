{
  "type": "flowchart",
  "title": "Thread Lifecycle and Scheduling States",
  "description": "A flowchart illustrating the different states a thread can be in and the transitions between them as managed by the operating system scheduler.",
  "elements": ["New", "Ready", "Running", "Blocked/Waiting", "Terminated"],
  "relationships": [
    "New -> Ready (admitted)",
    "Ready -> Running (scheduled)",
    "Running -> Ready (interrupted)",
    "Running -> Blocked/Waiting (I/O request)",
    "Blocked/Waiting -> Ready (I/O complete)",
    "Running -> Terminated (exit)"
  ]
}
