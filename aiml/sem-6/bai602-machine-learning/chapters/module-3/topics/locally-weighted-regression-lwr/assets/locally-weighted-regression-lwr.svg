<svg width="420" height="400" data-topic-id="locally-weighted-regression-lwr">
  <title>Locally Weighted Regression (LWR)</title>
  <defs>
    <linearGradient id="pastel-gradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#C7B8EA" stop-opacity="1"/>
      <stop offset="100%" stop-color="#C5CAE9" stop-opacity="1"/>
    </linearGradient>
  </defs>

  <!-- Section 1: Introduction -->
  <rect x="50" y="50" width="320" height="100" fill="url(#pastel-gradient)" rx="10" data-narration="Locally Weighted Regression (LWR) is a type of regression algorithm that combines the benefits of traditional regression models with the adaptability of nearest-neighbor learning. It is a similarity-based learning approach that focuses on local neighborhoods to make predictions, rather than relying on global models.">
    <animateTransform attributeName="transform" type="translate" begin="0s" dur="2s" delay="0s" from="0 0" to="0 100" repeatCount="indefinite"/>
  </rect>

  <!-- Section 2: Key Concepts -->
  <rect x="50" y="160" width="320" height="100" fill="url(#pastel-gradient)" rx="10" data-narration="Locality: LWR focuses on local neighborhoods to make predictions. Weighted Regression: LWR uses weighted regression to calculate the predicted value at a given point. Similarity Measure: LWR uses a similarity measure to determine the neighborhood of a point. K-Nearest Neighbors: LWR can be viewed as a variant of the k-nearest neighbors algorithm.">
    <animateTransform attributeName="transform" type="translate" begin="0.5s" dur="2s" delay="0.5s" from="0 200" to="0 300" repeatCount="indefinite"/>
  </rect>

  <!-- Section 3: How LWR Works -->
  <rect x="50" y="270" width="320" height="100" fill="url(#pastel-gradient)" rx="10" data-narration="1. Similarity Measure: The algorithm calculates a similarity measure between the target point and a set of data points. 2. K-Nearest Neighbors: The algorithm identifies the k-nearest neighbors based on the similarity measure. 3. Weighted Regression: The algorithm calculates the weighted regression coefficients for the k-nearest neighbors. 4. Predicted Value: The algorithm calculates the predicted value at the target point by combining the weighted regression coefficients.">
    <animateTransform attributeName="transform" type="translate" begin="1s" dur="2s" delay="1s" from="0 350" to="0 450" repeatCount="indefinite"/>
  </rect>

  <!-- Section 4: Types of LWR -->
  <rect x="50" y="370" width="320" height="100" fill="url(#pastel-gradient)" rx="10" data-narration="Global LWR: This type of LWR uses a global similarity measure to determine the neighborhood. Local LWR: This type of LWR uses a local similarity measure to determine the neighborhood. Adaptive LWR: This type of LWR adapts the similarity measure and the neighborhood based on the data.">
    <animateTransform attributeName="transform" type="translate" begin="1.5s" dur="2s" delay="1.5s" from="0 500" to="0 600" repeatCount="indefinite"/>
  </rect>

  <!-- Section 5: Advantages and Disadvantages of LWR -->
  <rect x="50" y="470" width="320" height="100" fill="url(#pastel-gradient)" rx="10" data-narration="Advantages: Flexibility, robustness, and interpretability. Disadvantages: Computational complexity.">
    <animateTransform attributeName="transform" type="translate" begin="2s" dur="2s" delay="2s" from="0 650" to="0 750" repeatCount="indefinite"/>
  </rect>

  <!-- Footer -->
  <text x="50" y="490" font-size="24" fill="#3498db">Locally Weighted Regression (LWR)</text>
</svg>