<svg width="420" height="400"
     data-topic-id="different-ways-to-combine-classifiers"
     xmlns="http://www.w3.org/2000/svg"
     xmlns:xlink="http://www.w3.org/1999/xlink"
     xlink:href="#gradient-background">
  
  <!-- Header Section -->
  <rect x="10" y="10" width="400" height="50" fill="#2b7eb8"/>
  <text x="15" y="25" font-size="18" font-family="Arial" fill="#ffffff">Different Ways To Combine Classifiers</text>
  <animate xlink:href="#title-text" begin="0s" dur="2s" repeatCount="indefinite"/>
  
  <!-- Boosting Section -->
  <g id="boosting-section">
    <rect x="10" y="80" width="150" height="50" fill="#46b0fa"/>
    <text x="15" y="95" font-size="16" font-family="Arial" fill="#ffffff">Boosting</text>
    <animate xlink:href="#boosting-text" begin="0.2s" dur="2s" repeatCount="indefinite"/>
    <g id="boosting-illustration">
      <circle cx="50" cy="20" r="30" fill="#ffffff"/>
      <path d="M 50 20 L 100 30 L 150 20" stroke="#46b0fa" stroke-width="2"/>
    </g>
  </g>
  <g id="boosting-data-narration">
    <rect x="10" y="150" width="150" height="50" fill="#46b0fa" data-narration="Boosting is an ensemble learning technique where each classifier is weighted differently based on its performance. The goal is to assign higher weights to the classifiers that perform well and lower weights to those that perform poorly.">
      <animate xlink:href="#boosting-narration" begin="0.4s" dur="2s" repeatCount="indefinite"/>
    </rect>
    <text x="15" y="165" font-size="14" font-family="Arial" fill="#ffffff" data-narration="Boosting is an ensemble learning technique where each classifier is weighted differently based on its performance. The goal is to assign higher weights to the classifiers that perform well and lower weights to those that perform poorly."></text>
  </g>
  
  <!-- Adaboost Section -->
  <g id="adaboost-section">
    <rect x="10" y="210" width="150" height="50" fill="#2196f3"/>
    <text x="15" y="225" font-size="16" font-family="Arial" fill="#ffffff">Adaboost</text>
    <animate xlink:href="#adaboost-text" begin="0.6s" dur="2s" repeatCount="indefinite"/>
    <g id="adaboost-illustration">
      <circle cx="50" cy="20" r="30" fill="#ffffff"/>
      <path d="M 50 20 L 100 30 L 150 20" stroke="#2196f3" stroke-width="2"/>
    </g>
  </g>
  <g id="adaboost-data-narration">
    <rect x="10" y="270" width="150" height="50" fill="#2196f3" data-narration="Adaboost is a popular boosting algorithm that assigns weights to each classifier using the following formula: W_t = (1 + α \* epsilon_t) / 2, where W_t is the weight of the t-th classifier, α is the learning rate, and epsilon_t is the error of the t-th classifier.">
      <animate xlink:href="#adaboost-narration" begin="0.8s" dur="2s" repeatCount="indefinite"/>
    </rect>
    <text x="15" y="285" font-size="14" font-family="Arial" fill="#ffffff" data-narration="Adaboost is a popular boosting algorithm that assigns weights to each classifier using the following formula: W_t = (1 + α \* epsilon_t) / 2, where W_t is the weight of the t-th classifier, α is the learning rate, and epsilon_t is the error of the t-th classifier."></text>
  </g>
  
  <!-- Stumping Section -->
  <g id="stumping-section">
    <rect x="10" y="330" width="150" height="50" fill="#f7dc6f"/>
    <text x="15" y="345" font-size="16" font-family="Arial" fill="#ffffff">Stumping</text>
    <animate xlink:href="#stumping-text" begin="1s" dur="2s" repeatCount="indefinite"/>
    <g id="stumping-illustration">
      <circle cx="50" cy="20" r="30" fill="#ffffff"/>
      <path d="M 50 20 L 100 30 L 150 20" stroke="#f7dc6f" stroke-width="2"/>
    </g>
  </g>
  <g id="stumping-data-narration">
    <rect x="10" y="390" width="150" height="50" fill="#f7dc6f" data-narration="Stumping is a simple boosting technique where a single decision tree is used as a base classifier. A stump is a single leaf node in the decision tree, and multiple stumps are combined to create the final model.">
      <animate xlink:href="#stumping-narration" begin="1.2s" dur="2s" repeatCount="indefinite"/>
    </rect>
    <text x="15" y="405" font-size="14" font-family="Arial" fill="#ffffff" data-narration="Stumping is a simple boosting technique where a single decision tree is used as a base classifier. A stump is a single leaf node in the decision tree, and multiple stumps are combined to create the final model."></text>
  </g>
  
  <!-- Bagging Section -->
  <g id="bagging-section">
    <rect x="10" y="450" width="150" height="50" fill="#8bc34a"/>
    <text x="15" y="465" font-size="16" font-family="Arial" fill="#ffffff">Bagging</text>
    <animate xlink:href="#bagging-text" begin="1.4s" dur="2s" repeatCount="indefinite"/>
    <g id="bagging-illustration">
      <circle cx="50" cy="20" r="30" fill="#ffffff"/>
      <path d="M 50 20 L 100 30 L 150 20" stroke="#8bc34a" stroke-width="2"/>
    </g>
  </g>
  <g id="bagging-data-narration">
    <rect x="10" y="510" width="150" height="50" fill="#8bc34a" data-narration="Bagging (Bootstrap Aggregating) is an ensemble learning technique where multiple instances of the training dataset are created with replacement. Each instance is then used to train a separate classifier. The final model is created by taking a vote of the individual classifiers.">
      <animate xlink:href="#bagging-narration" begin="1.6s" dur="2s" repeatCount="indefinite"/>
    </rect>
    <text x="15" y="525" font-size="14" font-family="Arial" fill="#ffffff" data-narration="Bagging (Bootstrap Aggregating) is an ensemble learning technique where multiple instances of the training dataset are created with replacement. Each instance is then used to train a separate classifier. The final model is created by taking a vote of the individual classifiers."></text>
  </g>
  
  <!-- Subagging Section -->
  <g id="subagging-section">
    <rect x="10" y="570" width="150" height="50" fill="#e91e63"/>
    <text x="15" y="585" font-size="16" font-family="Arial" fill="#ffffff">Subagging</text>
    <animate xlink:href="#subagging-text" begin="1.8s" dur="2s" repeatCount="indefinite"/>
    <g id="subagging-illustration">
      <circle cx="50" cy="20" r="30" fill="#ffffff"/>
      <path d="M 50 20 L 100 30 L 150 20" stroke="#e91e63" stroke-width="2"/>
    </g>
  </g>
  <g id="subagging-data-narration">
    <rect x="10" y="630" width="150" height="50" fill="#e91e63" data-narration="Subagging is a variant of bagging where the number of instances used to train each classifier is reduced. This is done to reduce the computational cost of training multiple classifiers.">
      <animate xlink:href="#subagging-narration" begin="2s" dur="2s" repeatCount="indefinite"/>
    </rect>
    <text x="15" y="645" font-size="14" font-family="Arial" fill="#ffffff" data-narration="Subagging is a variant of bagging where the number of instances used to train each classifier is reduced. This is done to reduce the computational cost of training multiple classifiers."></text>
  </g>
  
  <!-- Random Subagging Section -->
  <g id="random-subagging-section">
    <rect x="10" y="690" width="150" height="50" fill="#9c27b0"/>
    <text x="15" y="705" font-size="16" font-family="Arial" fill="#ffffff">Random Subagging</text>
    <animate xlink:href="#random-subagging-text" begin="2.2s" dur="2s" repeatCount="indefinite"/>
    <g id="random-subagging-illustration">
      <circle cx="50" cy="20" r="30" fill="#ffffff"/>
      <path d="M 50 20 L 100 30 L 150 20" stroke="#9c27b0" stroke-width="2"/>
    </g>
  </g>
  <g id="random-subagging-data-narration">
    <rect x="10" y="750" width="150" height="50" fill="#9c27b0" data-narration="Random subagging is a type of subagging where the instances used to train each classifier are randomly selected from the original dataset.">
      <animate xlink:href="#random-subagging-narration" begin="2.4s" dur="2s" repeatCount="indefinite"/>
    </rect>
    <text x="15" y="765" font-size="14" font-family="Arial" fill="#ffffff" data-narration="Random subagging is a type of subagging where the instances used to train each classifier are randomly selected from the original dataset."></text>
  </g>
  
  <!-- Footer Section -->
  <rect x="10" y="810" width="400" height="50" fill="#2b7eb8"/>
  <text x="15" y="825" font-size="18" font-family="Arial" fill="#ffffff">Footer</text>
  <animate xlink:href="#footer-text" begin="3s" dur="2s" repeatCount="indefinite"/>
  
</svg>