<svg viewBox="0 0 800 600" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style>
      .step { opacity: 0; }
      text { font-family: Arial, sans-serif; }
      .title { font-size: 24px; font-weight: bold; fill: #1F2937; }
      .label { font-size: 14px; fill: #374151; }
      .description { font-size: 12px; fill: #6B7280; }
      .box { stroke: #E5E7EB; stroke-width: 2; }
      .arrow { fill: none; stroke: #9CA3AF; stroke-width: 2; marker-end: url(#arrowhead); }
    </style>
    <marker id="arrowhead" markerWidth="10" markerHeight="10" refX="9" refY="3" orient="auto">
      <polygon points="0 0, 10 3, 0 6" fill="#9CA3AF" />
    </marker>
  </defs>

  <!-- Step 0: Title -->
  <g id="step0" class="step" data-narration="Unix File System Organization">
    <text x="400" y="50" class="title" text-anchor="middle">Unix File System Organization</text>
  </g>

  <!-- Step 1: Introduction to the Unix File System -->
  <g id="step1" class="step" data-narration="Introduction to the Unix File System: The Unix file system is a hierarchical, tree-structured meth">
    <rect x="150" y="120" width="280" height="80" rx="8" fill="#3B82F6" opacity="0.1" class="box" />
    <text x="290" y="150" class="label" text-anchor="middle" font-weight="bold">Introduction to the Unix File System</text>
    <text x="290" y="175" class="description" text-anchor="middle">The Unix file system is a hierarchical, tree-struc</text>
  </g>

  <g id="step1a" class="step" data-narration="Moving to the next concept">
    <path d="M 290 200 L 590 230" class="arrow" />
  </g>

  <!-- Step 2: The Root Directory (`/`) -->
  <g id="step2" class="step" data-narration="The Root Directory (`/`): The very top of the file system hierarchy is the **root dire">
    <rect x="450" y="230" width="280" height="80" rx="8" fill="#EC4899" opacity="0.1" class="box" />
    <text x="590" y="260" class="label" text-anchor="middle" font-weight="bold">The Root Directory (`/`)</text>
    <text x="590" y="285" class="description" text-anchor="middle">The very top of the file system hierarchy is the *</text>
  </g>

  <g id="step2a" class="step" data-narration="Moving to the next concept">
    <path d="M 590 310 L 290 340" class="arrow" />
  </g>

  <!-- Step 3: Basic File Types -->
  <g id="step3" class="step" data-narration="Basic File Types: In Unix, files are categorized into several types. You can i">
    <rect x="150" y="340" width="280" height="80" rx="8" fill="#F97316" opacity="0.1" class="box" />
    <text x="290" y="370" class="label" text-anchor="middle" font-weight="bold">Basic File Types</text>
    <text x="290" y="395" class="description" text-anchor="middle">In Unix, files are categorized into several types.</text>
  </g>

  <g id="step3a" class="step" data-narration="Moving to the next concept">
    <path d="M 290 420 L 590 450" class="arrow" />
  </g>

  <!-- Step 4: Standard Directory Hierarchy (FHS) -->
  <g id="step4" class="step" data-narration="Standard Directory Hierarchy (FHS): The Filesystem Hierarchy Standard (FHS) defines the structur">
    <rect x="450" y="450" width="280" height="80" rx="8" fill="#EC4899" opacity="0.1" class="box" />
    <text x="590" y="480" class="label" text-anchor="middle" font-weight="bold">Standard Directory Hierarchy (FHS)</text>
    <text x="590" y="505" class="description" text-anchor="middle">The Filesystem Hierarchy Standard (FHS) defines th</text>
  </g>

  <g id="step4a" class="step" data-narration="Moving to the next concept">
    <path d="M 590 530 L 290 560" class="arrow" />
  </g>

  <!-- Step 5: The Home Directory and the `$HOME` Varia -->
  <g id="step5" class="step" data-narration="The Home Directory and the `$HOME` Varia: Every user on a Unix system is assigned a personal directory">
    <rect x="150" y="560" width="280" height="80" rx="8" fill="#F97316" opacity="0.1" class="box" />
    <text x="290" y="590" class="label" text-anchor="middle" font-weight="bold">The Home Directory and the `$HOME` Varia</text>
    <text x="290" y="615" class="description" text-anchor="middle">Every user on a Unix system is assigned a personal</text>
  </g>

  <g id="step5a" class="step" data-narration="Moving to the next concept">
    <path d="M 290 640 L 590 670" class="arrow" />
  </g>

  <!-- Step 6: The `PATH` Variable -->
  <g id="step6" class="step" data-narration="The `PATH` Variable: The `PATH` is a crucial environment variable that tells the ">
    <rect x="450" y="670" width="280" height="80" rx="8" fill="#EC4899" opacity="0.1" class="box" />
    <text x="590" y="700" class="label" text-anchor="middle" font-weight="bold">The `PATH` Variable</text>
    <text x="590" y="725" class="description" text-anchor="middle">The `PATH` is a crucial environment variable that </text>
  </g>

  <g id="step6a" class="step" data-narration="Moving to the next concept">
    <path d="M 590 750 L 290 780" class="arrow" />
  </g>

  <!-- Step 7: Pathnames: Absolute vs. Relative -->
  <g id="step7" class="step" data-narration="Pathnames: Absolute vs. Relative: A **pathname** is a text string that specifies the location ">
    <rect x="150" y="780" width="280" height="80" rx="8" fill="#F97316" opacity="0.1" class="box" />
    <text x="290" y="810" class="label" text-anchor="middle" font-weight="bold">Pathnames: Absolute vs. Relative</text>
    <text x="290" y="835" class="description" text-anchor="middle">A **pathname** is a text string that specifies the</text>
  </g>
</svg>