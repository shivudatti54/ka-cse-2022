{
  "type": "flowchart",
  "title": "UNIX passwd Command Flow: Authentication & File Interaction",
  "description": "A flowchart diagramming the process flow of the passwd command, highlighting its interaction with the /etc/passwd and /etc/shadow files, and the role of system calls and setuid.",
  "elements": [
    "User",
    "passwd command (setuid root)",
    "/etc/passwd file",
    "/etc/shadow file",
    "Kernel/System Calls"
  ],
  "relationships": [
    "User executes command",
    "Command checks file permissions and ownership",
    "Command reads from /etc/passwd",
    "Command writes to /etc/shadow",
    "Kernel mediates secure access"
  ]
}