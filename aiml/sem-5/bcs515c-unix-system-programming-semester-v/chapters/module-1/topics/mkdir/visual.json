{
  "type": "flowchart",
  "title": "System Call Flow for mkdir()",
  "description": "A flowchart showing the process flow and kernel interaction when a user program calls the mkdir() system call.",
  "elements": [
    "User Program",
    "C Library (libc)",
    "mkdir() System Call",
    "Kernel VFS",
    "Underlying Filesystem (e.g., ext4)",
    "New Directory on Disk"
  ],
  "relationships": [
    "User Program calls libc wrapper",
    "libc wrapper invokes syscall instruction",
    "Kernel handles syscall, VFS validates path and permissions",
    "VFS calls specific filesystem's mkdir operation",
    "Filesystem writes directory structure to disk"
  ]
}