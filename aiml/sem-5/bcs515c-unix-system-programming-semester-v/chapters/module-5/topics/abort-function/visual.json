{
  "type": "flowchart",
  "title": "Process of the abort() Function",
  "description": "A flowchart illustrating the sequence of events and signal handling when the abort() function is called in a UNIX program.",
  "elements": [
    "Program calls abort()",
    "SIGABRT signal sent to process",
    "Signal handler (if installed)",
    "Process termination",
    "Core dump file creation (if enabled)"
  ],
  "relationships": [
    "abort() sends SIGABRT to the current process",
    "If a custom SIGABRT handler exists, it is executed",
    "After handler execution, abort() terminates the process",
    "Termination includes creating a core dump if configured"
  ]
}