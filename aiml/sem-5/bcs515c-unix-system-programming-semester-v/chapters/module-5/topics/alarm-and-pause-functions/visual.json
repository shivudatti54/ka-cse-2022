{
  "type": "timeline",
  "title": "Interaction of Alarm and Pause Functions",
  "description": "A timeline showing the sequence of events when a process uses alarm(), pause(), and receives a SIGALRM signal.",
  "elements": [
    "Process calls alarm(n)",
    "Process enters pause() state",
    "SIGALRM signal is delivered",
    "Signal handler executes (if set)",
    "pause() returns, process resumes"
  ],
  "relationships": [
    "alarm(n) starts a timer",
    "pause() blocks the process until any signal is received",
    "After n seconds, the kernel delivers SIGALRM",
    "Signal delivery interrupts pause()",
    "Process continues execution after signal handler completes"
  ]
}