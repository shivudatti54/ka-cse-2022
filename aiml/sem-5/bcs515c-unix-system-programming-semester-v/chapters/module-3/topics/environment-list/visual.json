{
  "type": "flowchart",
  "title": "UNIX Process Environment List",
  "description": "Illustrates how a process inherits and maintains its environment variables in UNIX",
  "elements": [
    "Parent Process",
    "Child Process",
    "Environment List (char **environ)",
    "Environment Variables (KEY=VALUE pairs)"
  ],
  "relationships": [
    "Parent passes environment list to Child during fork()",
    "Child can modify its own copy of environment list",
    "Environment list is stored as an array of strings in memory"
  ]
}