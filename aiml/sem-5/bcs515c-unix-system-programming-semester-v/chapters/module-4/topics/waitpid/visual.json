{
  "type": "flowchart",
  "title": "Process State Flow with waitpid()",
  "description": "A flowchart showing how the parent process uses waitpid() to monitor and synchronize with a specific child process, handling its termination and obtaining its exit status.",
  "elements": [
    "Parent Process",
    "Child Process (PID=X)",
    "Zombie Process",
    "waitpid() System Call",
    "Exit Status"
  ],
  "relationships": [
    "Parent calls fork() to create Child",
    "Child executes and eventually terminates",
    "Child becomes a Zombie process",
    "Parent calls waitpid(PID=X) to reap the specific Child",
    "waitpid() returns the Child's PID and Exit Status to Parent, removing the Zombie"
  ]
}