{
  "type": "flowchart",
  "title": "popen() Function Process Flow",
  "description": "Illustrates the sequence of operations when a C program uses the popen() function to create a pipe and execute a shell command.",
  "elements": [
    "Calling Process",
    "popen() Library Function",
    "Shell (/bin/sh)",
    "Child Process",
    "Command (e.g., ls, grep)",
    "Pipe"
  ],
  "relationships": [
    "Calling Process invokes popen()",
    "popen() forks a Child Process",
    "Child Process execs Shell",
    "Shell interprets and executes Command",
    "A Pipe is created between Calling Process and Child Process for data flow"
  ]
}