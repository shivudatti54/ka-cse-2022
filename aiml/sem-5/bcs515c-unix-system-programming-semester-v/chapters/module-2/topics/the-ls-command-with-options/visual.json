{
  "type": "flowchart",
  "title": "Decision Tree for Common `ls` Command Options",
  "description": "A flowchart that guides a user on which `ls` option to use based on their specific information need (e.g., view hidden files, see details, sort by time).",
  "elements": [
    "User Goal",
    "ls -l (Long Format)",
    "ls -a (All Files)",
    "ls -t (Time Sort)",
    "ls -r (Reverse Order)",
    "ls -lh (Human-Readable Sizes)",
    "Terminal Output"
  ],
  "relationships": [
    "User goal determines the path through the flowchart",
    "Options can be combined (e.g., `ls -lath`)",
    "Each path leads to a specific terminal output format"
  ]
}