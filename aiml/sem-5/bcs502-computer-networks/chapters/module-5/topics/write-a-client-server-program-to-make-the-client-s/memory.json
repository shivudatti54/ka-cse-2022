{
  "mnemonics": ["Client-Server Connection", "File Sharing Protocol", "Request-Response Cycle"],
  "keyPoints": [
    "Client sends file name to server over network",
    "Server checks if file exists in local storage",
    "If file exists, server sends file contents to client",
    "If file doesn't exist, server sends 'Not Found' message to client",
    "Client-server protocol uses TCP/IP for communication",
    "File sharing protocol uses HTTP for request-response cycle"
  ]
}
