<svg width="420" height="400" data-topic-id="tcpip-sockets" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
  <!-- gradient background -->
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#73A5F5" stop-opacity="1" />
      <stop offset="100%" stop-color="#C0E8F8" stop-opacity="1" />
    </linearGradient>
  </defs>
  <rect x="10" y="10" width="400" height="380" fill="url(#grad)" rx="10" />
  <!-- title -->
  <text x="20" y="20" font-size="30" font-family="Arial" font-weight="bold" fill="#0C093F">
    <tspan data-narration="Introduction">TCP/IP Sockets</tspan>
  </text>
  <!-- overview -->
  <rect x="10" y="60" width="400" height="50" fill="#F7F7F7" rx="10" />
  <text x="20" y="70" font-size="20" font-family="Arial" fill="#000">
    <tspan data-narration="Overview">In this section, we will explore the concept of TCP/IP sockets and their usage in computer networking.</tspan>
  </text>
  <!-- what is a socket -->
  <rect x="10" y="110" width="400" height="50" fill="#F7F7F7" rx="10" />
  <g transform="translate(10, 170)">
    <circle cx="20" cy="20" r="15" fill="#F7F7F7" />
    <rect x="-10" y="-20" width="30" height="30" fill="#F7F7F7" rx="10" />
    <text x="20" y="40" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="Socket Address">Socket Address</tspan>
    </text>
    <text x="20" y="60" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="This is the address of the socket, which includes the IP address and port number of the device it belongs to.">This is the address of the socket, which includes the IP address and port number of the device it belongs to.</tspan>
    </text>
    <text x="20" y="80" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="Socket">Socket</tspan>
    </text>
    <text x="20" y="100" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="This is the endpoint itself, which is used to send and receive data.">This is the endpoint itself, which is used to send and receive data.</tspan>
    </text>
  </g>
  <!-- types of sockets -->
  <rect x="10" y="170" width="400" height="50" fill="#F7F7F7" rx="10" />
  <g transform="translate(10, 230)">
    <rect x="-10" y="-20" width="30" height="30" fill="#F7F7F7" rx="10" />
    <rect x="-10" y="-30" width="30" height="30" fill="#F7F7F7" rx="10" />
    <text x="20" y="40" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="Stream Sockets">Stream Sockets</tspan>
    </text>
    <text x="20" y="60" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="These are used for connection-oriented communication, where a connection is established between the sender and receiver before data can be sent.">These are used for connection-oriented communication, where a connection is established between the sender and receiver before data can be sent.</tspan>
    </text>
    <text x="20" y="80" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="Datagram Sockets">Datagram Sockets</tspan>
    </text>
    <text x="20" y="100" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="These are used for connectionless communication, where data is sent independently and does not require a connection to be established before transmission.">These are used for connectionless communication, where data is sent independently and does not require a connection to be established before transmission.</tspan>
    </text>
  </g>
  <!-- tcpip socket basics -->
  <rect x="10" y="310" width="400" height="50" fill="#F7F7F7" rx="10" />
  <g transform="translate(10, 370)">
    <rect x="-10" y="-20" width="30" height="30" fill="#F7F7F7" rx="10" />
    <rect x="-10" y="-30" width="30" height="30" fill="#F7F7F7" rx="10" />
    <rect x="-10" y="-40" width="30" height="30" fill="#F7F7F7" rx="10" />
    <text x="20" y="40" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="Socket Structure">Socket Structure</tspan>
    </text>
    <text x="20" y="60" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="This includes the socket address and socket.">This includes the socket address and socket.</tspan>
    </text>
    <text x="20" y="80" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="Socket Options">Socket Options</tspan>
    </text>
    <text x="20" y="100" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="These are used to customize the behavior of the socket.">These are used to customize the behavior of the socket.</tspan>
    </text>
    <text x="20" y="120" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="Buffer">Buffer</tspan>
    </text>
    <text x="20" y="140" font-size="20" font-family="Arial" fill="#000">
      <tspan data-narration="This is used to store data that is being sent or received.">This is used to store data that is being sent or received.</tspan>
    </text>
  </g>
  <!-- title at bottom -->
  <text x="20" y="380" font-size="30" font-family="Arial" font-weight="bold" fill="#0C093F">
    <tspan data-narration="TCP/IP Sockets"></tspan>
  </text>
</svg>