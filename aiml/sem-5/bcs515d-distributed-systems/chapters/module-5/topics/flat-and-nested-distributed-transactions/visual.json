{
  "type": "flowchart",
  "title": "Flat vs. Nested Distributed Transactions",
  "description": "A flowchart comparing the simple, single-level structure of a flat transaction with the hierarchical, multi-level structure of a nested transaction.",
  "elements": [
    "Client Application",
    "Coordinator (Flat)",
    "Participant A (Database)",
    "Participant B (Service)",
    "Sub-Coordinator (Nested)",
    "Participant C (Database)",
    "Participant D (Service)"
  ],
  "relationships": [
    "Flat: Client sends one transaction to a coordinator which manages two participants",
    "Nested: Client sends a transaction that contains a sub-transaction; the coordinator delegates the sub-transaction to a sub-coordinator, which then manages its own participants"
  ]
}