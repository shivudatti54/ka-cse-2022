{
  "mnemonics": ["Oscar Overwrites All", "Eeny Environment Variables Only"],
  "keyPoints": [
    "Ordinary variables are stored in the shell's memory and are overwritten when the shell restarts.",
    "Environment variables are stored outside the shell's memory and persist between shell sessions.",
    "Ordinary variables are used to store values that are not meant to be shared across multiple commands.",
    "Environment variables are used to store values that are meant to be shared across multiple commands and shell sessions."
  ]
}
