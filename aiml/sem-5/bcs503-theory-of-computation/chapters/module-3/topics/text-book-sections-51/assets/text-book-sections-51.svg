<svg width="420" height="400" data-topic-id="text-book-sections-51" viewBox="0 0 420 400">
  <defs>
    <linearGradient id="gradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#C9E4CA" stop-opacity="1"/>
      <stop offset="50%" stop-color="#D7F7F7" stop-opacity="1"/>
      <stop offset="100%" stop-color="#E6F7F7" stop-opacity="1"/>
    </linearGradient>
  </defs>
  <rect x="10" y="10" width="400" height="360" fill="url(#gradient)"/>

  <title>Text Book Sections 51</title>

  <!-- Section 1: Introduction to Formal Language -->
  <g transform="translate(10, 50)">
    <rect width="350" height="30" fill="#E6F7F7" rx="5" data-narration="A formal language is a set of strings over an alphabet, where each string is called a word. A formal language is defined as follows: An alphabet is a set of symbols, denoted by Σ. A word is a string of symbols over the alphabet, denoted by s. A formal language is a set of words, denoted by L. For example, let Σ = {0, 1} be the alphabet, and let L = {0, 01, 1001} be a formal language.">
      <animateTransform attributeName="translate" from="0,50" to="0,100" dur="2s" begin="0s" repeatCount="indefinite"/>
    </rect>
    <text x="10" y="70" fill="#333" font-size="16">A formal language is a set of strings over an alphabet...</text>
    <line x1="10" y1="70" x2="10" y2="100" stroke="#666" stroke-width="2" marker-end="url(#arrow)"/>
  </g>

  <!-- Section 2: Properties of Formal Language -->
  <g transform="translate(10, 100)">
    <rect width="350" height="30" fill="#E6F7F7" rx="5" data-narration="Formal languages have several important properties: Closure, Decidability, and Regularity. A formal language is closed under a particular operation, such as union, intersection, or concatenation. A formal language is decidable if there exists an algorithm that can determine whether a given word is in the language or not. A formal language is regular if it can be described by a regular expression.">
      <animateTransform attributeName="translate" from="0,100" to="0,110" dur="2s" begin="0s" repeatCount="indefinite"/>
    </rect>
    <text x="10" y="130" fill="#333" font-size="16">Formal languages have several important properties...</text>
    <line x1="10" y1="130" x2="10" y2="150" stroke="#666" stroke-width="2" marker-end="url(#arrow)"/>
    <line x1="10" y1="130" x2="10" y2="170" stroke="#666" stroke-width="2" marker-end="url(#arrow)"/>
  </g>

  <!-- Section 3: Regular Expressions -->
  <g transform="translate(10, 150)">
    <rect width="350" height="30" fill="#E6F7F7" rx="5" data-narration="Regular expressions are a way to describe formal languages using a formal grammar. A regular expression is a string of symbols that describes a pattern of words. For example, the regular expression `0*1*` describes the formal language L = {0, 01, 1001}, where `*` denotes any number of consecutive symbols.">
      <animateTransform attributeName="translate" from="0,150" to="0,160" dur="2s" begin="0s" repeatCount="indefinite"/>
    </rect>
    <text x="10" y="190" fill="#333" font-size="16">Regular expressions are a way to describe formal languages...</text>
    <line x1="10" y1="190" x2="10" y2="210" stroke="#666" stroke-width="2" marker-end="url(#arrow)"/>
  </g>

  <!-- Section 4: Types of Regular Expressions -->
  <g transform="translate(10, 220)">
    <rect width="350" height="30" fill="#E6F7F7" rx="5" data-narration="There are two types of regular expressions: Deterministic and Nondeterministic. Deterministic regular expressions describe a formal language by specifying a set of possible words. Nondeterministic regular expressions describe a formal language by specifying a set of possible strings.">
      <animateTransform attributeName="translate" from="0,220" to="0,230" dur="2s" begin="0s" repeatCount="indefinite"/>
    </rect>
    <text x="10" y="260" fill="#333" font-size="16">There are two types of regular expressions...</text>
    <line x1="10" y1="260" x2="10" y2="280" stroke="#666" stroke-width="2" marker-end="url(#arrow)"/>
    <line x1="10" y1="260" x2="10" y2="300" stroke="#666" stroke-width="2" marker-end="url(#arrow)"/>
  </g>

  <!-- Footer -->
  <text x="10" y="360" fill="#333" font-size="16">Text Book Sections 51</text>
</svg>