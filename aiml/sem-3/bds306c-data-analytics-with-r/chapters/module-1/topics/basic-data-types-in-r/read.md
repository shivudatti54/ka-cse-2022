r
    # Assigning numeric values
    x <- 10.5   # A real number
    y <- 10     # Although an integer, it's stored as numeric by default
    class(x)    # Output: "numeric"
    class(y)    # Output: "numeric"