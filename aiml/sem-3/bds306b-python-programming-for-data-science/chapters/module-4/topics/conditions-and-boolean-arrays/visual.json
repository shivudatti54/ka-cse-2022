{
  "type": "flowchart",
  "title": "Conditional Filtering of a NumPy Array using Boolean Indexing",
  "description": "A flowchart demonstrating the process of applying a condition to a NumPy array to create a boolean mask, which is then used to filter the original array and return only the elements that meet the condition.",
  "elements": [
    "Original NumPy Array",
    "Condition (e.g., > 5)",
    "Boolean Mask Array (True/False)",
    "Filtered Result Array"
  ],
  "relationships": [
    "Condition is applied to each element",
    "Resulting boolean mask aligns with original array",
    "Mask is used to index and select only True elements"
  ]
}