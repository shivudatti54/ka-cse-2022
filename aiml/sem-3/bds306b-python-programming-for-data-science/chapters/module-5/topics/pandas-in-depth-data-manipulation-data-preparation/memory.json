{
  "mnemonics": [
    "Mike's Crazy Aunts Need Data (MCAND)",
    "Peculiar Animals Don't Like Dirty (PANDL)"
  ],
  "keyPoints": [
    "Missing Data? Use .isnull() to find out",
    "Concatenate data using .merge()",
    "Discretize data using .cut()",
    "Binning: Use .quantile() to divide data into bins",
    "Permute data: Use .sample(frac=0.2, replace=True) to shuffle data",
    "String manipulation: Use .str.lower() to convert to lowercase",
    "Data types: Use .dtype to check data type"
  ]
}
