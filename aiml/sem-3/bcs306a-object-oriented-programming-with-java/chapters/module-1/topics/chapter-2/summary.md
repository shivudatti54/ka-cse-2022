# Revision Notes: Chapter 2 - Object-Oriented Programming with JAVA

## Introduction

- Object-Oriented Programming (OOP) is a programming paradigm that revolves around the concept of objects and classes.
- Java is an object-oriented language that supports the OOP paradigm.

## Key Concepts

- **Abstraction**: The process of hiding internal implementation details and exposing only necessary information.
- **Encapsulation**: The concept of bundling data and methods that operate on that data within a single unit, called a class or object.
- **Inheritance**: The mechanism by which one class can inherit the properties and behavior of another class.
- **Polymorphism**: The ability of an object to take on multiple forms, depending on the context in which it is used.

## Types of Inheritance

- **Single Inheritance**: A class inherits from one parent class.
- **Multiple Inheritance**: A class inherits from multiple parent classes.
- **Multilevel Inheritance**: A class inherits from a parent class, which in turn inherits from another parent class.

## Interface vs. Abstract Class

- **Interface**: A abstract class that cannot be instantiated and is used to define a contract or a set of methods that must be implemented by any class that implements it.
- **Abstract Class**: A class that cannot be instantiated and is used to provide a partial implementation of a class.

## Important Formulas and Definitions

- **Object-Oriented Programming (OOP) Principles**:
  - **Abstraction**: "Hide implementation details and expose only necessary information."
  - **Encapsulation**: "Bundle data and methods that operate on that data within a single unit."
  - **Inheritance**: "Create a new class based on an existing class."
  - **Polymorphism**: "Take on multiple forms, depending on the context in which it is used."
- **Java Keyword**:
  - **public**: Access modifier, allows access from anywhere.
  - **private**: Access modifier, allows access only within the class.
  - **protected**: Access modifier, allows access within the class and its subclasses.

## Important Theorems

- **The First Law of Inheritance**: "Nothing can be added to a subclass, but a subclass can have all the fields and methods of its superclass."
- **The Second Law of Inheritance**: "A subclass can have different fields and methods than its superclass, but it cannot have more fields or methods than its superclass."

## Quick Revision Tips

- Review the OOP principles and concepts.
- Practice implementing classes, objects, and inheritance.
- Understand the differences between interfaces and abstract classes.
