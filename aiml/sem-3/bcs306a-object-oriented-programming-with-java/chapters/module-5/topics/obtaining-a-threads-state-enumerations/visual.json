{
  "type": "state_diagram",
  "title": "Java Thread Lifecycle State Transitions",
  "description": "A state diagram showing the possible states of a Java Thread and the transitions between them, as defined by the Thread.State enumeration.",
  "elements": [
    "NEW",
    "RUNNABLE",
    "BLOCKED",
    "WAITING",
    "TIMED_WAITING",
    "TERMINATED"
  ],
  "relationships": [
    "Thread.start() -> NEW to RUNNABLE",
    "Object.wait() -> RUNNABLE to WAITING",
    "Thread.sleep() -> RUNNABLE to TIMED_WAITING",
    "Synchronized lock acquired -> RUNNABLE to BLOCKED",
    "Run method completes -> RUNNABLE to TERMINATED",
    "Object.notify()/notifyAll() -> WAITING to RUNNABLE",
    "Sleep/timeout ends -> TIMED_WAITING to RUNNABLE"
  ]
}