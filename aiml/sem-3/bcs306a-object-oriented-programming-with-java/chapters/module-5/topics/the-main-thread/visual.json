{
  "type": "flowchart",
  "title": "The Java Main Thread Lifecycle",
  "description": "A flowchart illustrating the creation, execution, and termination of the main thread in a Java application, showing its relationship with the JVM and the call stack.",
  "elements": [
    "Java Virtual Machine (JVM)",
    "Main Thread",
    "main() method call stack",
    "Garbage Collector Thread"
  ],
  "relationships": [
    "JVM starts the Main Thread",
    "Main Thread executes main() method",
    "main() method calls create a stack frame",
    "Main Thread terminates after main() exits",
    "Garbage Collector runs concurrently as a daemon thread"
  ]
}