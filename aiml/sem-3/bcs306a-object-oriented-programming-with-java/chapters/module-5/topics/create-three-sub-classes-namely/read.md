java
class Superclass {
    // superclass members (fields and methods)
}

class Subclass extends Superclass {
    // subclass members (fields and methods)
    // plus all non-private members of Superclass
}