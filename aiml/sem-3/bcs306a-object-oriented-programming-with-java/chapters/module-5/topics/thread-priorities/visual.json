{
  "type": "diagram",
  "title": "Java Thread Priority Hierarchy",
  "description": "A visual representation of the Java thread priority constants and their numerical values, showing how the scheduler uses these priorities.",
  "elements": [
    "Thread.MIN_PRIORITY (1)",
    "Thread.NORM_PRIORITY (5)",
    "Thread.MAX_PRIORITY (10)",
    "JVM Scheduler",
    "CPU Core"
  ],
  "relationships": [
    "MIN_PRIORITY to NORM_PRIORITY represents the lower end of the scheduling preference",
    "NORM_PRIORITY to MAX_PRIORITY represents the higher end of the scheduling preference",
    "Scheduler uses priority values to decide execution order",
    "Higher priority threads get more CPU time but are not guaranteed to run first"
  ]
}