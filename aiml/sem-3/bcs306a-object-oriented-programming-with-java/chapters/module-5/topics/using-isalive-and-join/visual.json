{
  "type": "flowchart",
  "title": "Thread Lifecycle and Synchronization with isAlive() and join()",
  "description": "A flowchart showing how a main thread spawns a child thread, uses isAlive() to check its status, and uses join() to wait for its completion.",
  "elements": [
    "Main Thread",
    "Child Thread (MyRunnable)",
    "isAlive() Check",
    "join() Call"
  ],
  "relationships": [
    "Main Thread starts Child Thread",
    "Main Thread queries Child Thread status with isAlive()",
    "Main Thread blocks and waits for Child Thread termination with join()"
  ]
}