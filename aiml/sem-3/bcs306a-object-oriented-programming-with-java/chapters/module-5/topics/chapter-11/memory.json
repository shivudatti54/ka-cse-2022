{
  "mnemonics": ["Mother Needs to Inspect New Toys", "Major Nations Invade Just About Everything"],
  "keyPoints": [
    "The main thread is the primary thread of execution in a Java program.",
    "A thread is a separate flow of execution within a program, with its own program counter and stack.",
    "A thread can be created using the Thread class, which extends the Runnable interface or implements the Runnable interface directly."
  ]
}
