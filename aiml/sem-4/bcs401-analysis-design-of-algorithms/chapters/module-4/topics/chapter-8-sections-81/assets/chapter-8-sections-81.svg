<svg width="420" height="400" data-topic-id="dynamic-programming-three-basic-examples-and-the-knapsack-problem">
  <title>Chapter 8: Dynamic Programming - Three Basic Examples and The Knapsack Problem</title>
  <body>
    <div style="background: linear-gradient(to bottom, #7A658C, #A7D8F7);">
      <section data-narration="Introduction to Dynamic Programming">
        <h2>Dynamic Programming</h2>
        <p style="color: #B68E8E;">Dynamic programming is a method for solving complex problems by breaking them down into smaller subproblems, solving each subproblem only once, and storing the solutions to subproblems to avoid redundant computation.</p>
      </section>
      <section data-narration="Key Concepts">
        <h2>Key Characteristics</h2>
        <div style="display: flex; flex-direction: column; align-items: center;">
          <div style="width: 50px; height: 50px; background-color: #7A658C; border-radius: 50%; margin: 10px;"></div>
          <p style="color: #B68E8E;">Optimal substructure</p>
          <p style="color: #B68E8E;">Overlapping subproblems</p>
        </div>
      </section>
      <section data-narration="The Fibonacci Sequence">
        <h2>Fibonacci Sequence</h2>
        <p style="color: #B68E8E;">The Fibonacci sequence is a classic example of a problem that can be solved using dynamic programming.</p>
        <pre style="background-color: #A7D8F7; padding: 10px;">
          def fibonacci(n):
            fib = [0] * (n + 1)
            fib[1] = 1
            for i in range(2, n + 1):
              fib[i] = fib[i-1] + fib[i-2]
            return fib[n]
        </pre>
      </section>
      <section data-narration="Longest Common Subsequence">
        <h2>Longest Common Subsequence</h2>
        <p style="color: #B68E8E;">The longest common subsequence problem is a classic example of a problem that can be solved using dynamic programming.</p>
        <pre style="background-color: #A7D8F7; padding: 10px;">
          def longest_common_subsequence(X, Y):
            m = len
            dp = [[0] * (len(Y) + 1) for _ in range(len(X) + 1)]
            for i in range(1, len(X) + 1):
              for j in range(1, len(Y) + 1):
                if X[i-1] == Y[j-1]:
                  dp[i][j] = dp[i-1][j-1] + 1
                else:
                  dp[i][j] = max(dp[i-1][j], dp[i][j-1])
            return dp[-1][-1]
        </pre>
      </section>
      <section data-narration="The Knapsack Problem">
        <h2>The Knapsack Problem</h2>
        <p style="color: #B68E8E;">The knapsack problem is a classic example of a problem that can be solved using dynamic programming.</p>
        <pre style="background-color: #A7D8F7; padding: 10px;">
          def knapsack(weights, values, capacity):
            n = len(weights)
            dp = [[0] * (capacity + 1) for _ in range(n + 1)]
            for i in range(1, n + 1):
              for j in range(1, capacity + 1):
                if weights[i-1] <= j:
                  dp[i][j] = max(values[i-1] + dp[i-1][j-weights[i-1]], dp[i-1][j])
                else:
                  dp[i][j] = dp[i-1][j]
            return dp[n][capacity]
        </pre>
      </section>
      <section data-narration="Conclusion">
        <h2>Conclusion</h2>
        <p style="color: #B68E8E;">Dynamic programming is a powerful technique for solving complex problems by breaking them down into smaller subproblems and storing the solutions to subproblems to avoid redundant computation.</p>
      </section>
      <footer>
        <p style="text-align: center;">Chapter 8: Dynamic Programming - Three Basic Examples and The Knapsack Problem</p>
      </footer>
    </div>
  </body>
</svg>