<svg width="420" height="400" data-topic-id="warshalls-and-floyds-algorithms">
  <!-- Gradient background with pastel colors -->
  <defs>
    <linearGradient id="pastel-gradient" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#C5E1F5" stop-opacity="1"/>
      <stop offset="50%" stop-color="#E0E0E5" stop-opacity="1"/>
      <stop offset="100%" stop-color="#C7B8EA" stop-opacity="1"/>
    </linearGradient>
  </defs>
  <rect x="0" y="0" width="420" height="400" fill="url(#pastel-gradient)"/>

  <!-- Title section -->
  <section data-narration="Introduction">
    <h1>Warshall's and Floyd's Algorithms</h1>
    <p>Dynamic programming is a method for solving complex problems by breaking them down into smaller sub-problems, solving each sub-problem only once, and storing the solutions to sub-problems to avoid redundant computation.</p>
  </section>

  <!-- Warshall's Algorithm section -->
  <section data-narration="Warshall's Algorithm">
    <h2>Warshall's Algorithm</h2>
    <p>Warshall's algorithm is used to find the shortest path between all pairs of vertices in a weighted graph.</p>
    <ul>
      <li>Key Concepts:</li>
      <li>Weighted graph: A graph where each edge has a weight or label associated with it.</li>
      <li>Shortest path: The path with the minimum total weight between two vertices.</li>
      <li>Transitivity: The property that if there is a path from vertex A to vertex B and a path from vertex B to vertex C, then there is a path from vertex A to vertex C.</li>
    </ul>
    <p>Example:</p>
    <table>
      <thead>
        <tr>
          <th>From</th>
          <th>To</th>
          <th>Weight</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>A</td>
          <td>B</td>
          <td>2</td>
        </tr>
        <tr>
          <td>A</td>
          <td>C</td>
          <td>3</td>
        </tr>
        <tr>
          <td>B</td>
          <td>C</td>
          <td>1</td>
        </tr>
        <tr>
          <td>B</td>
          <td>D</td>
          <td>4</td>
        </tr>
        <tr>
          <td>C</td>
          <td>D</td>
          <td>2</td>
        </tr>
      </tbody>
    </table>
  </section>

  <!-- Floyd's Algorithm section -->
  <section data-narration="Floyd's Algorithm">
    <h2>Floyd's Algorithm</h2>
    <p>Floyd's algorithm is used to find the shortest path between all pairs of vertices in a weighted graph.</p>
    <p>It is particularly useful for finding the shortest path in a directed acyclic graph (DAG).</p>
  </section>

  <!-- Implementation section -->
  <section data-narration="Implementation">
    <h2>Implementation</h2>
    <pre>
    def warshall(W):
      n = len(W)
      for k in range(n):
        for i in range(n):
          for j in range(n):
            if W[i][k] + W[k][j] < W[i][j]:
              W[i][j] = W[i][k] + W[k][j]
    </pre>
  </section>

  <!-- Animation section -->
  <section data-narration="Animation">
    <h2>Animation</h2>
    <svg width="420" height="400">
      <circle cx="210" cy="150" r="10" fill="#007bff" stroke="#fff" stroke-width="2"/>
      <line x1="210" y1="150" x2="210" y2="350" stroke="#007bff" stroke-width="2"/>
    </svg>