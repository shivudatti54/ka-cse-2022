{
  "title": "Create a user and grant all permissions to theuser",
  "type": "flowchart",
  "elements": [
    "Create a new user",
    "Grant SELECT, INSERT, UPDATE, DELETE permissions",
    "Grant EXECUTE, CREATE, DROP, INDEX, PRAGMA permissions",
    "Log in as the new user",
    "Test the new user's permissions"
  ],
  "connections": [
    "Create a new user -> Grant SELECT, INSERT, UPDATE, DELETE permissions",
    "Grant SELECT, INSERT, UPDATE, DELETE permissions -> Grant EXECUTE, CREATE, DROP, INDEX, PRAGMA permissions",
    "Grant EXECUTE, CREATE, DROP, INDEX, PRAGMA permissions -> Log in as the new user",
    "Log in as the new user -> Test the new user's permissions"
  ]
}
