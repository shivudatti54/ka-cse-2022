{
  "topicId": "chomsky-hierarchy-grammars",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Context-Free Grammar Parser",
      "description": "Top-down parsing using recursion",
      "code": "FUNCTION parse_S():\n  IF next_token == 'a' THEN\n    consume('a')\n    parse_S()\n  ELSE\n    consume('b')\nEND",
      "explanation": "Implements S \u2192 aS | b production rules",
      "timeComplexity": "O(n)",
      "spaceComplexity": "O(n) (stack depth)"
    }
  ]
}