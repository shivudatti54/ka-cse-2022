{
  "topicId": "skip-list",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "Skip List Search",
      "description": "Demonstrates top-down search pattern",
      "code": "function search(key):\n    current = header\n    for level in reverse(top_level to 0):\n        while current.forward[level].key < key:\n            current = current.forward[level]\n    current = current.forward[0]\n    return current if current.key == key else null",
      "explanation": "Starts from highest level, moves right while possible, then descends levels",
      "timeComplexity": "O(log n)",
      "spaceComplexity": "O(n)"
    }
  ]
}