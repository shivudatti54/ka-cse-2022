{
  "topicId": "multilayer-feedforward-networks",
  "flashcards": [
    {
      "id": "fc-1",
      "front": "Define Multilayer Feedforward Neural Network and explain its architecture.",
      "back": "A Multilayer Feedforward Neural Network consists of an input layer, one or more hidden layers, and an output layer. Information flows in one direction (forward) from input through hidden layers to output, with each neuron receiving inputs from previous layer and producing output for next layer. Unlike perceptrons, hidden layers enable the network to learn non-linear patterns."
    },
    {
      "id": "fc-2",
      "front": "Explain the Backpropagation algorithm with the chain rule.",
      "back": "Backpropagation computes gradients of loss function w.r.t. weights by propagating errors backward from output to input layer. Using chain rule: ∂E/∂W^l = ∂E/∂a^l × ∂a^l/∂z^l × ∂z^l/∂W^l. The gradient is used to update weights via gradient descent: W_new = W_old - η × ∂E/∂W, where η is learning rate."
    },
    {
      "id": "fc-3",
      "front": "Compare ReLU and Sigmoid activation functions with respect to vanishing gradient problem.",
      "back": "Sigmoid squashes values to (0,1) but has gradient near 0 for very large/small inputs, causing vanishing gradient problem in deep networks. ReLU f(x)=max(0,x) is computationally efficient, promotes sparsity, and avoids vanishing gradient for positive inputs. However, ReLU can cause 'dying neuron' problem when gradients are consistently negative."
    },
    {
      "id": "fc-4",
      "front": "Differentiate between MSE and Cross-Entropy loss functions. When is each used?",
      "back": "MSE (Mean Squared Error) = (1/n)Σ(y_pred - y_true)² is used for regression tasks, penalizes large errors heavily. Cross-Entropy = -Σy_true × log(y_pred) is used for classification, measures distribution divergence and provides stronger gradients for wrong predictions. Choice depends on output type: continuous→MSE, categorical→Cross-Entropy."
    },
    {
      "id": "fc-5",
      "front": "List and explain key hyperparameters in training feedforward networks.",
      "back": "Key hyperparameters: (1) Learning Rate - controls weight update magnitude; (2) Batch Size - samples processed before update; (3) Epochs - complete dataset passes; (4) Momentum - accelerates convergence by adding past gradient fraction; (5) Number of Hidden Layers/Neurons - determines model capacity. Proper tuning prevents underfitting/overfitting."
    },
    {
      "id": "fc-6",
      "front": "Write the mathematical formulation for forward propagation in a feedforward network.",
      "back": "For layer l: z^l = W^l × a^(l-1) + b^l, where W^l is weight matrix, b^l is bias vector. Then a^l = σ(z^l), where σ is activation function. For entire network: output = σ(W³×σ(W²×σ(W¹×x+b¹)+b²)+b³). The weights W and biases b are learnable parameters initialized before training."
    },
    {
      "id": "fc-7",
      "front": "Compare SGD, Momentum, and Adam optimizers used in training feedforward networks.",
      "back": "SGD updates weights using only current batch gradient (slow convergence). Momentum adds velocity term: v = γv + η∇W, reduces oscillations. Adam computes adaptive learning rates using first (momentum) and second (RMSProp) moment estimates: better for sparse gradients and non-convex optimization. Adam often preferred for deep networks due to faster convergence."
    },
    {
      "id": "fc-8",
      "front": "What are the advantages and limitations of Multilayer Feedforward Networks?",
      "back": "Advantages: Universal function approximator (can learn any continuous function), handles complex non-linear patterns, widely applicable (classification, regression, pattern recognition). Limitations: Requires large training data, computationally expensive, prone to overfitting without regularization (dropout, early stopping), no built-in memory for sequential data, requires careful architecture design (trial-and-error)."
    }
  ]
}