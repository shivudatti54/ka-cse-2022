{
  "topicId": "file-organizations-index-structures",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "B+ Tree Search",
      "description": "Algorithm for key lookup",
      "code": "FUNCTION search(key):\n    current = root\n    WHILE current not leaf:\n        FIND first i where key <= current.key[i]\n        current = current.child[i]\n    RETURN current.record[key]",
      "explanation": "Traverses from root to leaf using key comparisons",
      "timeComplexity": "O(log n)",
      "spaceComplexity": "O(1)"
    }
  ]
}