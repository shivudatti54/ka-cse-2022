{
  "topicId": "dns-dhcp-bootp",
  "examples": [
    {
      "id": "code-1",
      "language": "pseudocode",
      "title": "DNS Recursive Query",
      "description": "Basic DNS resolution algorithm",
      "code": "FUNCTION resolve(domain):\n   IF cache has domain:\n       RETURN cached IP\n   ELSE:\n       query root server\n       follow referrals until authoritative server\n       cache result\n       RETURN IP",
      "explanation": "Demonstrates recursive lookup with caching",
      "timeComplexity": "O(log n)",
      "spaceComplexity": "O(n)"
    }
  ]
}