{
  "topicId": "modular-exponentiation",
  "examples": [
    {
      "id": "code-1",
      "language": "Python",
      "title": "Modular Exponentiation",
      "description": "Iterative implementation using exponentiation by squaring",
      "code": "def mod_exp(base, exp, mod):\n    result = 1\n    base = base % mod\n    while exp > 0:\n        if exp % 2 == 1:\n            result = (result * base) % mod\n        exp = exp // 2\n        base = (base * base) % mod\n    return result",
      "explanation": "Processes bits of exponent from LSB to MSB using squaring and conditional multiplication",
      "timeComplexity": "O(log exp)",
      "spaceComplexity": "O(1)"
    }
  ]
}